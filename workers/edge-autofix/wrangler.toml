# Template config — per-client instances override `name` and add their own
# `routes` at onboarding (one isolated worker per client domain, doc 04 §5).
name = "edge-autofix-template"
main = "src/index.ts"
compatibility_date = "2026-07-01"

# Per-client route example (set at client onboarding, Phase 1.3):
# routes = [{ pattern = "clientdomain.com/*", zone_name = "clientdomain.com" }]
