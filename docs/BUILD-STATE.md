# BUILD-STATE — live task board
### Maintained by: lead-architect-orchestrator (state) + documentation (freeze log)

> The Orchestrator drives from `07-build-sequence-and-execution-checklist.md`. This file records where the build actually is. Do not cross a 🔒 gate marked ⏸ below.

## Current stage: **PHASE 1 OPEN — foundation frozen; operator live. SHIPPED & gated (2026-07-09): real-data slice · plan-regeneration slice · ALL FOUR 1.3 write methods (8/8 gates PASS) · 1.4 M2 Audit Engine + M3 Visibility Tracker (Code Review PASS post-SSRF-remediation). In progress: rest of 1.4 (M5 crawler/render monitoring, M4 competitor citations, M6 content decay). Standing preconditions: ticket A (vertical-aware ensurePlan supersede) before a 2nd vertical activates; write-methods wiring preconditions + crawler DNS-rebinding socket-pinning before any live-property/multi-tenant exposure.**

> **Provisioning note (2026-07-08):** the frozen F1 foundation schema (migrations 0001–0006) was applied to the operator's live Supabase project via the SQL Editor (the remote build environment's network policy blocks direct DB access, so the operator ran a combined script). Verified live: all 13 public tables present with `relrowsecurity` AND `relforcerowsecurity` = true. App-runtime connection secrets (`NEXT_PUBLIC_SUPABASE_URL`, anon key, etc.) to be wired when the first data-backed screen is built. Network policy note: this environment blocks outbound to Supabase/Vercel/GitHub API; allowlisting `*.supabase.co` would let the orchestrator manage the DB directly (operator/admin change).

> **Provisioning note 2 (2026-07-09) — auth layer LIVE:** with `api.supabase.com` allowlisted and an operator-supplied short-lived access token, the orchestrator provisioned the live project directly via the Supabase Management API (per `ops/environments.md` §Authentication): (1) migrations **0007 + 0008 applied** (verified: `auth_hooks.custom_access_token_hook` + `app.user_role` exist); (2) **Custom Access Token hook ENABLED** via auth config (`pg-functions://postgres/auth_hooks/custom_access_token_hook`); (3) operator auth user created (email confirmed); (4) **tenant #1 "The Design Lab"** created + operator wired as `agency_admin` in `tenant_users`. **End-to-end verified:** a real password sign-in against the live project returned a JWT carrying `tenant_id` + `user_role=agency_admin` with reserved `role=authenticated` — the hook mints claims exactly per contract §2/§12. The operator was instructed to revoke the access token immediately after. Remaining operator-local steps: `.env.local` (URL + anon key), `npm run dev`, sign in, change temp password.

| Step | Item | Owner | Gate | Status |
|---|---|---|---|---|
| 0.0 | Agent team + 4 skills operationalized (doc 01) | operator + orchestrator | — | ✅ Done (2026-07-07) — 12 agents in `.claude/agents/`, 4 skills in `.claude/skills/` |
| 0.1 | Project scaffold (Next.js + Supabase + Vercel, CI/CD, staging/prod, secrets, CF Workers readiness) | devops-deployment | Code Review | ✅ Done (2026-07-07) — Code Review **PASS** (see gate record below); operator-side provisioning steps listed in `ops/environments.md` |
| 0.2 | 4 skills implemented + tested in isolation (gate tests the LIBRARY, per resolution 3) | build agents | Code Review + QA | ✅ Done (2026-07-07) — gate CLOSED after remediation + green re-verification (see gate record) |
| 0.3 | Multi-tenant data model (F1) | lead-backend-data-architect | Code Review (security+isolation) + QA isolation suite | ✅ Done (2026-07-07) — **GATE CLOSED**, doc 03 §7 criteria 1–5 all MET (see gate record); freeze declaration awaits operator at Freeze Gate 0 |
| 0.4 | Design system + theming (F2) | lead-ui-ux-designer | Design Review + **operator sign-off** (resolution 4b) | ✅ Done — Design Review passed, B1 blocker fixed + re-review CLEARED (369 tests), **operator signed off 2026-07-08. FROZEN.** |
| 🔒 | **FREEZE GATE 0** — foundation frozen | orchestrator | all of 0.2–0.4 passed; F1 + F2 presented to operator | ✅ **PASSED 2026-07-08** — operator signed off both F1 and F2 after a local walkthrough. Foundation FROZEN; changes now require Orchestrator + Code Review sign-off. |
| 1.1 | Industry Playbook Engine (M1) — build first | aeo-seo-logic-engineer | Code Review + Content Quality | ⏭️ Next up — Phase 1 open |
| 1.x | Remaining Phase 1 feature work | (per doc 07) | (per doc 07) | ▶️ Unblocked |
| 1.1 | Industry Playbook Engine (M1) + onboarding flow | aeo-seo-logic-engineer (+frontend/designer) | Code Review + Content Quality | ✅ Done (2026-07-08) — both gates PASS; M1b deferred to M18-infra availability (see gate record) |
| 1.2 | Change-management layer + provider-agnostic connector interfaces | integrations-engineer | Code Review (rollback safety) + QA (rollback suite) | ✅ Done (2026-07-08) — BOTH gates PASS (0 blockers); all findings remediated (see gate record). 651 unit + 385 isolation green. |
| 1.3 | Auto-fix write methods (WordPress→Webflow→Wix→CF edge worker) implementing WriteMethodAdapter | integrations-engineer | Code Review + QA (rollback per method) | ✅ **DONE (2026-07-09) — all four methods, both gates PASS each.** Adapters library-complete; NOT yet wired to live properties (ticket ii — carries HARD preconditions: per-property construction AND per-property write serialization w/ proof test, + first-live-connect canaries per method). |
| 🔒 | **GATE 1a** — single-vertical validation (real estate, GG as client zero, full loop end-to-end) | orchestrator | loop proven + case-study metrics | 🚫 Not reached |
| 🔒 | **GATE 1** — Phase 1 ships multi-vertical | orchestrator | validated loop across seed verticals + M1b active | 🚫 Not reached |

## Post-freeze foundation changes (require Orchestrator + Code Review sign-off — CLAUDE.md rule 1)

**2026-07-08 · F2 token-system extension: add a SECONDARY + WARM accent · Orchestrator AUTHORIZED · Code Review → PASS (0 blockers, 0 majors, 1 minor)**
Code Review CLOSED the change: confirmed truly additive (7 frozen tokens byte-unchanged; theming engine, 5 moments, component library untouched), B1 CSS-injection defense intact for the new token paths (14 hostile `accent_secondary`/`accent_warm` payloads — brace-breakout, `</style>`, `url()`, `expression()` — all caught by the hex gate → Signal fallback), accessibility gate correctly applied (Alachua `#f4a200`→`#c18000` real + reported; Core Orange `#fc4c14` genuinely at ~3.14:1, gated not waved through), backward-compatible (pre-existing tenant rows without the fields still resolve via Signal default). Minor (stale `serialize.ts` header comment) fixed. **The two-accent extension is signed off.** Note: this does NOT re-freeze F2 — the operator's brand look itself is still a first pass awaiting operator reaction; a re-freeze of the operator theme happens only on operator sign-off.
Original authorization:
Reason: the operator's real brand (tenant #1, `docs/design/operator-brand.md`) has **two** headline colors — Core Blue `#1B2FCE` (primary) and Core Orange `#FC4C14` (secondary/energy) — plus Alachua `#F4A200` (warm) and Dark Blue `#0A1C46`. The frozen F2 token set carries a single `--accent`. To represent the brand faithfully, the token system is extended **additively**: add `--accent-secondary` and `--accent-warm` to `ColorTokens`/`DesignTokenSet` + `buildBrandKit` (accessibility-gated like the existing accent) + `toCssVariables`. The existing 7 tokens, the theming engine, the 5 moments, and the component library are unchanged; this is purely additive. Owner: `lead-ui-ux-designer`. **Not "done" until Code Review signs off** (security + no-regression on the frozen brand-kit skill) — this is the sanctioned controlled-change path, not a re-opening of the freeze. Reconciliation: the operator's brand guide is authoritative; the earlier Webflow-blue proxy (`#146EF5`) is discarded.

## Gate records

**2026-07-09 · Phase 1.4 (part) — M2 Audit Engine + M3 Visibility Tracker · Code Review → REJECT (SSRF) → remediated → PASS**
Commit 9e83459 + SSRF remediation commit. M2 wraps the frozen aeo-audit skill (bounded same-origin crawler → skill scoring → fixes flow into the real generatePlan/audit-merge → tenant-scoped `audits` persistence); M3 samples AI-engine citations via the CitationDataProvider port → documented score formula (⚑ operator-ratify) → append-only `visibility_results`. No client-facing text → Content Quality not triggered (confirmed). Tenant isolation, honesty (no invented numbers; absent≠0; per-page/coverage structural), telemetry redaction all verified clean first pass. **Code Review Major — SSRF: the crawler fetched attacker-influenceable property URLs with only a protocol check, so internal/link-local/RFC1918/cloud-metadata targets were reachable server-side (redirect- and link-pivot escapes were already closed; the direct-internal-target path was open + unflagged). ORCHESTRATOR DISPOSITION: CLOSE THE CLASS.** New `crawl/egress-guard.ts` blocks the full private/loopback/link-local/ULA/CGNAT/metadata range set (IPv4 + IPv6 incl. mapped/compat unwrap), classifies the WHATWG-canonicalized host undici connects to (so decimal/octal/hex re-encodings can't create a guard-vs-connect mismatch), literal-sync then DNS via a REQUIRED injected resolver (any-blocked-wins, fail-closed), wired at every fetch entry; blocked → uniform `blocked_address`, no status oracle. Re-review attacked 23 literal encodings + 10 DNS cases → 0 leaks; PASS. Minors fixed: NUL byte in derive.ts; per-request 15s timeout + 120s crawl budget. 1793/1793 green.
**Tickets:** (M2/M3-i) **DNS-rebinding TOCTOU** is the documented residual — undici socket-pinning to the vetted IP MUST land before broad multi-tenant/white-label crawler exposure (hard precondition, like the write-methods wiring); (ii) trailing-dot FQDN `localhost.` hardening nit (sync check; DNS path already fail-closes it); (iii) pre-existing NUL byte in `write-methods/cloudflare/qa-rollback.test.ts` (1.3 territory, functional, separate cleanup pass). **⚑ Launch-threshold flags for pre-Gate-1a ratification:** crawl bounds 30 pages / depth 3 / 1.5 MB; request timeout 15s; wall budget 120s; RENDER_VISIBLE_MIN_WORDS=30; M3 visibility score formula; M3 frozen-schema limits (no run-metadata/coverage history, no per-market column).

**2026-07-09 · Phase 1.3 method 4/4 — Cloudflare edge worker · Code Review → REJECT → remediated → PASS · QA rollback gate → PASS (re-confirmed post-remediation) — 1.3 BLOCK COMPLETE**
Commits 7feb288 (+ remediation & QA-suite commit). The API-less method: a per-client-domain worker rewrites HTML in flight (HTMLRewriter) from a versioned KV rules manifest; the adapter writes rule slots, never origin content. Inversions documented + pinned: before-capture = rule state (null first-class); rollback STRONGER than methods 1-3 (removal reveals the untouched origin); verification manifest-level only — render effect belongs to MONITOR (~60s KV propagation + primed-cache/304 caveats). Worker fail-open proven by response-object identity on every failure path (one carve-out honestly documented: origin-fetch rejection has no origin response to serve); injection escaping breakout-proof under hostile probing; rewritten responses no-store + validators + surrogate-control-class headers stripped.
**CR Major 1 — the stale-base manifest race** (two same-property operations read the same base; both verify byte-exact; the first write is silently lost — a lost REVERT would leave a rule live while its row reads reverted): KV has no CAS, but ALL manifest writers are in-house, so — **ORCHESTRATOR + CODE REVIEW DISPOSITION: KNOWN RACE with a HARD PRECONDITION — this method must not be wired to live client properties until per-property write serialization exists at the wiring seam, arriving with a serialization proof test (QA's binding expectation).** Prose corrected everywhere; the race pinned by a deterministic two-writer interleaving test that re-opens the disposition if adapter-level detection ever improves. **CR Major 2 — bare `title` selector would have clobbered inline-SVG accessibility titles page-wide:** fixed via head-scoped selectors applied at the sole production entry (compile-time-exhaustive mapping, reviewer-accepted seam) + an independent headClosed handler gate proven under bare selectors. QA re-confirmed PASS post-remediation (97/97 suite untouched; 1638/1638 full).
**First-live-deploy canaries (consolidated, in-code + here):** real scoped selectors against real lol-html; CF error-code semantics (10009/403); raw KV byte echo; KV values-GET read-after-write consistency. Non-gating backlog: duplicate JSON-LD on pathological multi-`</head>` markup; map-or-throw hardening in scopeRewriterSelectors.

**2026-07-09 · Phase 1.3 method 3/4 — Wix write method · Code Review → REJECT → remediated → PASS (+ disposition sign-off) · QA rollback gate → FAIL → dispositioned + re-scoped → PASS**
Commits 350ef90 (+ remediation & QA-suite commit). Live-immediate honesty (no staged layer — before-capture + verified revert IS the safety story; applied_at correlation sound here, with the resumed-rows caveat). Grammar structurally kills the system-field class (no leading `_`, lookalike probes pinned) and app-namespaced collections; account-level keys make the wix-site-id pin the isolation boundary (on every request, GUID-validated, host exact-pinned). All three Webflow carries (α echo-class kill via shared refuseBaseUrl, β mirror-corner test, γ 429 pins) confirmed CLOSED — the credential-echo class is dead.
**Both gates independently found the same core defect (WIX-QA-2 = CR Major 1): a client-staff CMS edit landing inside the data write's GET→PUT window is silently overwritten (Wix Data v2 is last-writer-wins full replace — no conditional/partial update; the race cannot be closed at the transport). ORCHESTRATOR + CODE REVIEW DISPOSITION — ACCEPTED RESIDUAL:** overclaims corrected everywhere, prominent adapter-header residual section (window/why/size/operator-facing consequence: correlate item `_updatedDate` vs `applied_at`/`reverted_at` when a client reports a vanished edit), pinned in TWO independent suites (engineer's adapter pin + QA's pipeline pins, disposition-dated) so any behavioral change re-opens the disposition. CR Major 2 genuinely FIXED: page-SEO writes now verify the NON-target seoData attr from the echo against the pre-write capture (replace-semantics reality or unset-materialization → loud write_verification_failed; survived reviewer attacks incl. echo-omits-seoData). QA red-sensitivity: 7 injected defects across the two passes, all turned suites red (incl. both residual pins proven load-bearing, not vacuous).
**Carried:** (δ) pin the residual-test's fake hook via _updatedDate/journal so a broken hook can't make the pin vacuous (fold into 4/4 commit); (ε) first-live-write canary (pages merge semantics, data-item system-field requirements) — joins ticket (ii) per-property wiring; ticket (iii) stuck-previewed ops surface RE-FLAGGED as more acute for live-immediate Wix (a verification failure leaves the LIVE site divergent while the row reads previewed); operator-facing framing for the residual when Wix wiring UI ships.

**2026-07-09 · Phase 1.3 method 2/4 — Webflow write method · Code Review → PASS (0 blockers/majors, 4 minors) · QA rollback gate → PASS (no defects)**
Commit 042e1d8 (+ QA suite commit). Staged-only by design: no locator can express a publish; rollback restores the staged capture even under unpublished drift; live snapshot proven untouched via positive route whitelist + journal sweeps. Site pinned at the API level (page siteId asserted read/pre-write/echo; collections proven against the pinned site's list; api.webflow.com exact-pin closes SSRF). Mirrored-OG refusal double-checked pre-write AND post-write; slug fields refused as server-normalized-irreversible; 429 → new shared `rate_limited` code, no automatic retries. QA: 122 adversarial tests through the real ChangeManager (round-trip exhaustive w/ grammar-completeness pin, staged/published boundary under drift, QA-1 crash windows re-proven through this adapter, auto-rollback modes, partial batch w/ 429 + re-run continuation), red-sensitivity proven by 5 injected defects (each turned the suite red: 99/2/2/84/5 failures). Also fixed in 042e1d8: WP carried item (i) (unparseable-base-URL echo).
**Carried to Wix (3/4) commit — MANDATORY per reviewer (third carry = rejection):** (α) WP constructor's query/fragment branch still echoes the raw base URL (`?token=...` class) — hoist a shared refuse-without-echo helper so the class dies; (β) test the post-write mirror-flag branch (add FakeWebflow `setMirror()` + one-shot flip hook, retiring QA's harness reach-ins); (γ) ~10-line 429 pins for mid-batch + revert-direction row-status truth. **New product ticket (named owner needed before Webflow serves real clients): Webflow "applied" = staged-only — MONITOR/auto-rollback correlation keyed to applied_at is unsound for this method (live effect lands at the client's next publish), and operator UI needs "staged; goes live on next publish" framing + a publish-aware correlation signal.** Joins carried items (ii) per-property adapter wiring and (iii) stuck-previewed ops surface from the WP record.

**2026-07-09 · Plan-regeneration + duplicate-guard slice · Code Review → REJECT → remediated → PASS · Design Review → PASS w/ findings → re-check PASS (all closed)**
Commits be9b866 + 2267fed (build) + 10d0e82 + f8aafa3 + 5bc0bcb (remediation). Delivered: `regeneratePlanForClient` (idempotent, typed reasons), onboarding idempotency key (one UUID per run, becomes the row id — lost-response retries recover instead of duplicating), runtime input clamp, redacted plan-failure telemetry, dashboard Generate-plan control (agency_admin-only, live verticals, incl. zero-task residue), HEAD count:exact aggregates (no silent undercount at the PostgREST cap). **Code Review's Major (caught pre-merge): a replay after the operator EDITED the payload silently returned the original row while the UI claimed the edits saved. Fixed by Orchestrator-directed UPDATE-THROUGH reconciliation** — divergent replays update the row (RLS-scoped, sanitized values only) and a vertical change supersedes the replay-run's own plan before regenerating; update failure returns a distinct honest error, never ok-with-stale. Re-review attacked jsonEqual false-negatives, cross-tenant/clamp bypass, supersede over-reach, partial-failure coherence — PASS. Design re-check confirmed the full recovery loop (warning copy → dashboard → control), approved the 70/30 negative/ink error-text mix (all combos ≥4.83:1; sentinel pins why plain --negative fails Signal-light), count-up retarget, full-width errors, StatusAnnouncer a11y. 1005/1005 tests.
**Tickets from this slice:** (A) **GRADUATION CONDITION — must land before a second vertical goes live:** vertical-aware `ensurePlan` supersede (a triple-failure corner can strand a wrong-playbook tasked plan that the Generate control won't offer to fix; live→live vertical edits would widen it to a Major); (B) governance comment at the tasked-plan sweep should state its freshness-window assumption; (C) replace ACTIVE_VERTICALS array mutation in tests with vi.mock; (D) /clients page plan row + control before client books exceed the dashboard's 6 cards; (E) step-4 error heading variant for the unreachable case (polish); (F) systemic `--negative-text` 4.5:1-gated derived token via the post-freeze path (tactical color-mix shipped meanwhile).

**2026-07-09 · Phase 1.3 method 1/4 — WordPress write method · Code Review → REJECT → remediated → PASS · QA rollback gate → FAIL → fixed → PASS**
Commits 92a939a (adapter) + the remediation commit. First code that can write to a client site. Code Review round 1: 1 Major (http: base URLs accepted → Application Password would egress cleartext; fixed: HTTPS-only at construction, no opt-in, bypass-probed) + 5 minors (meta:[] PHP quirk → accurate target_missing; transport error detail whitelisted, never err.message; userinfo targets refused without echo; fake-WP registeredMeta fidelity mode + 400 envelope; FetchPort pinned redirect:"error"). **QA gate found the block's most important defect — QA-1: the 1.2 change-management manager corrupted the rollback baseline on apply-retry after a DB crash post-site-write (re-baselined diff.before to the change's own after → rollback became a verified no-op while the site kept the change). Invisible to the 1.2 suites (they fault-injected only the adapter, never the store); exposed by the first real adapter.**
**AUTHORIZED POST-REVIEW-PASS CHANGE to the reviewed 1.2 layer (freeze-log entry):** manager.ts apply() (+30/−9) + 1-line additive PipelineWarning union (`resumed_after_partial_apply`, types.ts). When the live re-read ≠ persisted previewed before AND = the change's own after, the persisted before stays the rollback baseline (safe in both indistinguishable cases: crash-resume and third-party-set-same-value) with a distinct warning; legitimate drift re-baselines unchanged. Orchestrator authorized; Code Review re-review attacked all four axes and signed off (fix proven load-bearing: the QA test necessarily fails against the pre-fix manager); QA re-verdict PASS with new pins: double-crash loop, warning distinctness, third-party twin, and a pinned honest residual (adapter-invalid previewed before → loud invalid_value on every retry, row never leaves previewed — backlog: ops surface for stuck-previewed rows). QA suite: 139 adversarial tests incl. exhaustive locator-kind round-trips with a grammar-completeness pin, auto-rollback policy modes (off/flag/execute), partial-batch prefix rollback.
**Carried from this block:** (i) unparseable-base-URL error still echoes the raw URL (one echo path left; fold into method 2/4's commit); (ii) production wiring must construct WordPress adapters PER PROPERTY (method-keyed MapAdapterRegistry + per-property pin fails closed via property_mismatch, but the wiring step must document/handle it); (iii) ops surface for stuck-previewed rows (QA residual). Next: Webflow (method 2/4).

**2026-07-09 · Real-data slice — plan/task persistence + live operator dashboard · Code Review → PASS · Design Review → REJECT → remediated → PASS**
Commits 2d01e32 (persistence) + cc30b4a (onboarding shows the persisted plan) + ea27d58 (live /dashboard, APP_HOME) + df85ca5/4da0330 (design remediation). **Code Review PASS first pass** (0 blockers/majors, 6 minors): independently re-verified the full claim chain down to frozen RLS/composite FKs; hostile-payload test proves smuggled roadmap/tenant fields are dead weight; automation clamp at the persistence seam sweeps all seed playbooks — 'auto' survives only if the generator emitted it. **Design Review REJECT (narrow, copy-honesty) → PASS after remediation**: visual system/tokens/motion/mobile/honesty-rule all passed first pass; 4 gate-blockers were copy/layout (stale "preview" framing on dormant verticals, phantom "retry from the dashboard" affordance, outdated "landing next" status, pipeline-card dead space) — all fixed + one remediation-introduced footnote overreach caught on re-check and amended. Reviewer drove every onboarding outcome live (saved+N tasks / dormant-vertical save / plan-write-failed / client-write-failed / slow-write hold) via a write-capable stub. 710/710 unit tests; no migrations touched.
**Carried tickets from this slice:** (a) duplicate-client footgun — lost-response retry has no idempotency key AND no plan-retry control exists; both land in the plan-regeneration slice (owner: lead-backend-data-architect + frontend); (b) dashboard aggregates silently truncate at the PostgREST ~1000-row cap — server-side aggregates before task volume grows; (c) runtime clamp for `locations` shape/length at the write seam; (d) redacted server-side telemetry for plan-write failures; (e) **vertical activation must backfill plans for already-saved dormant-vertical clients** (the approved copy promises it — owner: aeo-seo-logic-engineer, due with vertical activation); (f) chart tooltip hardcodes "Share of voice" (pre-existing; additive `seriesName` prop needs the post-freeze Orchestrator+Code Review path); (g) internal jargon sweep ("M3", "Phase 1.8") before any client/white-label exposure; (h) polish: prettify suggested client name, step-4 title truncation at 1280px, dead `h-full` on work-row cards.

**2026-07-08 · "Make it real" (Option A) — login + protected shell + client persistence · Code Review → REJECT → remediated → PASS**
Security check PASSED first pass: no cross-tenant/unauthorized client write constructible (tenant_id claim-sourced, requireRole("agency_admin"), RLS `clients_insert with check` backstop). REJECT was a robustness Major: env-unset 500'd every route + a false "never a 500" code comment. Remediated (non-throwing `tryGetPublicSupabaseConfig` guards middleware + verifiedClaims → fail closed graceful; signIn errors genericized server-side). Focused re-review PASS: boot-with-no-env proves 0 route 500s; env-SET security path byte-identical (cannot fail open); 675 unit + 399 isolation green. **Option A slice COMPLETE and reviewed — pending operator live-provisioning (apply migrations 0007–0008, enable hook, seed tenant #1 + login, set env) to sign into the live app.** Full loop built: login → protected shell → onboarding persists a tenant-scoped client → /clients reads it back. Deferred to next slice: plans/tasks persistence + operator dashboard reading them.


**2026-07-08 · Auth-claims + session layer · Code Review (security + tenant isolation, Extra-depth) → PASS — RATIFIED (0 blockers, 0 majors, 1 non-exploitable minor)**
Reviewer independently re-derived properties on a fresh scratch DB (applied 0001–0008, seeded two tenants, called the hook with forged events) rather than trusting the committed tests. All 7 adversarial checks: forgery dropped + re-derived; fail-closed confirmed; **Option 2 reserved-claim resolution ratified sound + complete** (proved a smuggled `role:agency_admin` in the reserved claim grants zero app authority — 0 rows); hook EXECUTE locked to supabase_auth_admin, search_path locked, adds no non-`authenticated` policy (posture invariant holds); session/guards read only signature-verified claims; viewer client_id integrity proven end-to-end; no secret/PII leakage. Minor: hook passes the incoming reserved `role` claim through (by design, proven non-exploitable — RLS reads only `user_role`, PostgREST SET ROLE needs membership); optional defense-in-depth, deferred. **This authorized frozen-foundation change is fully ratified (Orchestrator + Code Review).** 667 unit + 399 isolation green.

**2026-07-08 · Auth-claims + session layer (Option A "make it real", slice 1) · BUILT — awaiting Extra-depth Code Review security gate**
Authorized additive foundation change (migrations 0007 hook + 0008 helper): the Custom Access Token hook mints `user_role`/`tenant_id`/`client_id` SERVER-SIDE from `tenant_users` keyed off the authenticated `auth.uid()`; fails closed on no membership; discards/never-trusts incoming app claims; `client_viewer` structurally cannot influence its own claims. Session/guard helpers (`src/lib/auth/`) read verified claims via `auth.getClaims()`; refresh-only middleware; email+password server actions (no login UI yet — next slice).
**ESCALATION RESOLVED (Orchestrator decision + Code Review to verify):** the frozen RLS read the app role from JWT `role`, which is PostgREST's RESERVED db-role claim → would break the first logged-in data query. Orchestrator chose **Option 2** (app role → non-reserved `user_role` claim; PostgREST's `role` stays `authenticated`, the request role every policy targets AND every isolation test exercises — test fidelity was the deciding factor, over introducing grantable app Postgres roles that would run prod in an untested role context). Implemented as migration 0008 (`app.user_role()` reads `user_role`) + coordinated hook/harness/db.ts/parse-claims/docs rename. **All 399 isolation tests pass on the new claim — the rename preserved every isolation property (re-proven, not assumed).** 667 unit green. Frozen-foundation change logged here per CLAUDE.md rule 1; Code Review security gate ratifies next.
Provisioning (operator, docs/ops/environments.md): enable the Custom Access Token hook in the Supabase dashboard + create tenant #1 + operator agency_admin row (copy-paste SQL provided).


**2026-07-08 · 1.2 change-management layer · CLOSED — Code Review (rollback safety) PASS + QA (rollback suite) PASS + all findings remediated**
QA gate PASS: every rollback-safety invariant has a regression test proven to fail-red if its guard breaks (confirmed by injecting each bug); +6 tests incl. a real coverage hole (store optimistic-transition guard through RLS — DB CHECKs enforce field-presence, not transition direction). Remediation (integrations-engineer): [MAJOR] credential leak via util.inspect/console.log CLOSED (ES #private field + inspect.custom; test proves the secret prints on no standard logging path); [Minor 2] applyBatch re-asserts single-client; [Minor 3] drift-clobber + write-before-DB ordering documented in-code; [Minor 4] stub guard aligned to 3 more DB CHECKs. 651 unit + 385 isolation green. QA verified manager.ts byte-identical to committed after its regression-injection technique. Detail below:

**2026-07-08 · 1.2 change-management layer · Code Review (rollback safety) → PASS (0 blockers, 1 major, 5 minors)** — adversarial: reviewer attempted a bypass of all 9 doc 04 §2 hard requirements, none succeeded; verified against real RLS + CHECKs (640 unit + 384 isolation incl. 21 live-PG pipeline). QA rollback-suite gate still running (both must pass to close 1.2). Dispositions: **Major** (vendor credential leaks via util.inspect/console.log — connectors/types.ts) NON-BLOCKING for 1.2 (no live creds flow yet) but MUST land before the first live adapter in 1.3/1.4 → integrations-engineer. **Minor 1** (`site_changes.diff` gains `target` key) — RATIFIED 2026-07-08 (Orchestrator + Code Review, schema-safe); contract updated (data-model.md §3/§7). **Minor 2** (applyBatch re-assert single-client), **Minor 3** (document drift-clobber + write-before-DB ordering), Minors 4–5 (stub CHECK parity, tripwire factory-shape note) → batched into the post-QA remediation pass. This DB payload-convention clarification does not alter the frozen schema (CHECK only requires a jsonb object).


**2026-07-08 · 1.1 Industry Playbook Engine + onboarding · Code Review → PASS (0 blockers, 0 majors, 7 minors) · Content Quality → PASS (gate cleared) · 1.1 CLOSED**
Scope: 5 seed playbooks (doc 02-faithful, test-pinned incl. Gate 1a `ACTIVE_VERTICALS=["real-estate"]`), real plan generator (proportional channel allocation, deterministic — 50-run byte-identity probed; doc 03 §6 automation clamp verified on M10/M13), onboarding flow (dormant-vertical gate enforces Gate 1a in code; truth-aligned copy), 557/557 tests. Frozen-file string touch (onboarding-plan-reveal default subheading) EXPLICITLY SIGNED OFF (verified one-line diff). Post-freeze additions confirmed contract-preserving: dualModeTenantCss (both-or-nothing fallback; 12 hostile payloads refused — B1 lineage holds, verified against React 19 raw <style> rendering), entrance choreography (SSR-identical by construction, dual reduced-motion gates), mode toggle. Two independent gates converged on the same stub-generator bug earlier in the cycle — governance working as designed.
**Carried open items (non-gating, owners assigned):** [aeo-seo-logic-engineer] broaden auto-clamp beyond M10/M13 to all non-detection modules; stale RED docblock in audit-merge.test.ts; asAuditResult element validation; duplicate impact wording on audit-merged tasks (MUST land before audit path is user-visible in 1.2); schema_profile in one evidence string; module codes in operator diagnostics (before client-visible audit UI); nap_directories provenance comments; vertical-tuned audience nouns when dormant verticals activate. [designer/frontend] newline in scope-id sanitization (fold into existing F2 defense-in-depth ticket); entrance.tsx header comment (box-shadow bloom); mode-toggle external-writer desync; optional "we'll flag you" → "we'll let you know".
**M1b Playbook Generator:** deliberately deferred within 1.1 — needs the M18 Claude+web-search stack (Anthropic API key not yet provisioned); built when M18 infrastructure lands, activated only after Gate 1a per doc 07.


**2026-07-08 · Operator brand v1 consolidated app-wide (dual-palette) + motion signature · post-freeze additions logged**
Operator decisions: pass-3 blue-depth look adopted; BOTH palettes kept as the app's light/dark modes; structure locked (roles: primary blue, highlight cyan, red = urgency, glow language, Geist, hero bubble), VALUES float (font/color changes remain cheap; additive-color path proven). Implemented via additive `dualModeTenantCss` formatter — frozen engine/token contract byte-untouched; Signal fallback preserved both modes. Pass-2 Core Orange/Alachua retained as available accents, not leading.
**Post-freeze motion addition (operator-directed):** entrance choreography as the brand motion signature, framed inside frozen moment #5 — rise 14px + fade 520ms cubic-bezier(0.22,1,0.36,1), 90ms sibling stagger, hero glow bloom 800ms @ +420ms, counters/gauge +400ms after tile entry; pure CSS, SSR-identical markup, transforms/opacity only, dual reduced-motion gates (verified: instant final states). Logged against the freeze record per governance; code-review coverage folds into the 1.1 gate.
Phase-1 flag (standing): dual-palette `tenants.theme` schema extension owned by lead-backend-data-architect at M7.


**2026-07-07 · 0.4 F2 design system · Code Review (code quality/security, per CLAUDE.md rule 3) → REJECT (1 blocker, 4 minors) → remediated → re-review CLEARED**
Design Review (self-owned) passed; re-skin/token-parity/reduced-motion all verified genuine. Hostile-input probing found **[B1 BLOCKER] stored CSS injection via tenant font stacks** — font strings reached the emitted `<style>` verbatim on the white-label production path; a `}` breaks out of the rule and injects app-wide CSS for every viewer of that tenant. Confirmed with three live payloads.
**Remediation:** font-grammar whitelist gate (`src/lib/skills/brand-kit/font-stack.ts`) in the shared `resolveAndValidateTokens` pipeline — hostile stacks throw → Signal fallback (matches malformed-color behavior), covering both the `<style>` string sink and the inline-style path, and both `buildBrandKit` + `reviseKit`. 17 regression tests + doc correction.
**Focused re-review 2026-07-07: B1 CLEARED.** All three original exploits + a broad bypass hunt (CSS `\7d` escapes, NBSP, zero-width, RTL override, fullwidth punctuation, `expression(`, 5000-char stack) defeated against the real bundled engine — no run emits a second `{`/`}`, `<`, or `url(`; legit stacks byte-unchanged. 369 unit tests green. Latent sibling (hostile `typography.scale` via future M7 direct `buildBrandKit`, unreachable via `tenants.theme`) confirmed out of scope → Phase-1 follow-up ticket. **F2 code side clear for operator sign-off; freeze awaits operator (resolution 4b).**
Minors carried to Phase 1: named-CSS-color test guard, id-sanitization defense-in-depth, logo_url/custom_domain untrusted-field note, typography.scale grammar gate before M7.

**2026-07-07 · 0.3 F1 data model · QA isolation suite → GREEN (360/360, zero successful attack vectors) · Code Review (security + isolation) → PASS (0 blockers, 1 major, 3 minors)**
Security/isolation core SIGNED OFF: no cross-tenant leak, no unforced RLS, under 360 suite tests + 13 independent reviewer probes (composite-FK attacks, re-homing, forged claims, verdict-nulling sneak-past — all blocked). Freeze criteria 1–3 MET; criterion 4 PARTIAL (contract must be corrected + published by documentation agent); criterion 5 security portion signed off.
**[CR-MAJOR 1]** Schema permitted `automation_level='auto'` on `site_changes` (a fully-autonomous on-page publish path), contradicting doc 00 §2 / CLAUDE.md rule 5 / doc 04 §6 — and the DRAFT contract overclaimed that the schema prevented it. Disposition: docs are not silent → CHECK constraint excluding 'auto' from site_changes + contract §6 correction, dispatched to lead-backend-data-architect 2026-07-07; QA updates its blessing test; documentation agent publishes corrected contract; focused re-review then closes the gate. Surfaced for operator ratification at Freeze Gate 0 regardless.
**Remediation complete 2026-07-07:** CHECK landed (`site_changes_automation_level_allowed` excludes 'auto'; approval gate simplified to `site_changes_requires_approval`, no exemptions); QA suite updated and green at **363/363** (supersedes the 360 pre-remediation count — the exemption blessing became four prohibition tests, future-table tripwire now sweeps all four roles); contract corrected + **published v1.0.0** by documentation agent (criterion 4 MET; sync-verified against SQL, two §5 drift items fixed); TS mirror narrowed (`SiteChangeAutomationLevel` excludes 'auto') per documentation escalation, applied by orchestrator.
**Focused re-review 2026-07-07: PASS — GATE CLOSED.** Zero findings remaining. Original MAJOR 1 construction re-attempted on a fresh scratch DB as superuser: rejected on both INSERT and UPDATE routes; full auto-rollback lifecycle still expressible. All five doc 03 §7 freeze criteria explicitly MET. Freeze declaration reserved for the operator at Freeze Gate 0.
Minors: verdict-presence-not-pass note (contract), client_id claim-minting obligation line (contract), all-roles future-table tripwire (QA).

**2026-07-07 · 0.2 Skill libraries · Code Review → PASS (0 blockers, 3 majors, 7 minors) · QA → GAPS-FOUND (2 criticals, 4 minors)**
Suites at review time: 293/293 across 20 files; isolation verified empirically (suites pass with no network namespace); all four SKILL.md hard rules survived adversarial probing. Gate held open pending disposition of: [QA-C1] schema-generation rejection path untested → tests being added; [QA-C2] NaN channel-weight bug in aeo-audit fix prioritization → `Number.isFinite` fix + regression test; [CR-M1] `reviseKit` bypasses build-time structural validation → unified validate pipeline; [CR-M2] compliance seed-vertical overwrite + zero-rules-evaluated pass → overwrite protection + empty-ruleset fails closed; [CR-M3] public `serializeToScriptBlock` accepts rejected drafts → type-restricted to ready results. All five + gate minors dispatched 2026-07-07 to owning agents (schema-generation/aeo-audit → aeo-seo-logic-engineer; brand-kit → lead-ui-ux-designer; compliance → content-production-engineer).
**Gate CLOSED 2026-07-07:** all 5 criticals/majors + all minors remediated and regression-tested (regressions verified to fail pre-fix where applicable); re-verification green — 329/329 tests across 23 files (schema-generation 70, aeo-audit 81, brand-kit 87, compliance 91), lint/typecheck/build clean. One shared-contract change (`SpacingTokens.steps` → readonly) applied with orchestrator sign-off per escalation protocol. F2 contrast pair-set policy set by design lead (10 pairs, both surfaces) — carried to 0.4 and the F2 ratification list.

**2026-07-07 · 0.1 Project scaffold · Code Review → PASS** (0 blockers, 1 major, 4 minors).
- Major (process): 0.1 was marked done in this file before the review verdict landed. No file change was required (the review passed), but the standing correction is recorded: **gate status is written from the verdict, never ahead of it.**
- Minors, disposition: env server-only split (fixed — `src/lib/env.server.ts` with `server-only` guard), CI `permissions: contents: read` (fixed), `docs/ops/environments.md` CI-env wording (fixed), worker-types note for Phase 1.3 (documented in `workers/edge-autofix/README.md`; due at 1.3).

## Freeze log (frozen-foundation decisions + post-freeze changes)

**🔒 FREEZE GATE 0 — FOUNDATION FROZEN — 2026-07-08 (operator sign-off).**
The operator reviewed a local prototype build of F2 (`npm run dev` → `/design-system`) and signed off on **both** freezes:
- **F1 — multi-tenant data model** (doc 03): 6 migrations, RLS enabled + forced on all 13 tables, structural tenant-consistency via composite FKs, in-schema approval/review gates, `auto` publishing prohibited on `site_changes`. Code Review security gate CLOSED (0 leaks under 360+ probes); QA isolation suite 363/363. **FROZEN.**
- **F2 — design system + white-label theming** (doc 06): token system, tenant theming engine (untrusted-input-safe, B1 CSS-injection fix cleared), 22 token-mapped components, Recharts language, 5 reduced-motion-gated moments. Design Review passed + operator sign-off (resolution 4b). **FROZEN.**
- Skills (0.2) gate-closed; agent team (0.0) operational.

**Post-freeze change control now in force (CLAUDE.md rule 1):** any change to the frozen data model or design system requires **Orchestrator + Code Review sign-off**, recorded here.

_No post-freeze changes yet._

## Open escalations to operator

_None blocking. The "pending ratification" items below are logged for decision at their named checkpoints (F1 = 0.3 freeze, F2 = 0.4 freeze, operator = Freeze Gate 0 review)._

## Operator ratifications — 2026-07-07 (all four decision groups ratified wholesale)

The operator ratified all four groups. Recorded here as binding; the frozen-foundation change-control rule (CLAUDE.md rule 1) governs any future change.

1. **Auto-publish prohibition (F1 MAJOR 1 fix):** RATIFIED. `automation_level='auto'` structurally impossible on `site_changes`; every on-page/site write is `ai_draft_human_approve` or `human_only`.
2. **Access-control split:** RATIFIED. `platform_owner` reads zero rows through tenant-facing policies (ops via `service_role`); clients/tenant_users/tenants writes are `agency_admin`-only; `operator` writes module tables; `client_viewer` read-only + own-client.
3. **F2 theming policies:** RATIFIED. 10-pair contrast set (both surfaces); light-surface `#f4f6f9`; one-palette-per-tenant; fixed type scale (tenants swap colors + fonts only).
4. **Library default thresholds:** RATIFIED as launch defaults — see the ⚑ revisit flags below.

### ⚑ Thresholds to revisit once we have real client data (GG / real estate / Gate 1a)
These were ratified as reasonable launch defaults but are the ones most likely to want tuning against real-world signal — flagged per operator request:
- **aeo-audit review-velocity thresholds** (hyper-local ≥4 new/30d, recency ≤30d; semi-local ≥1/30d, ≤60d) — pure estimates until we see real review flow per vertical.
- **aeo-audit refresh-window default (90d)** — real cadence is vertical- and client-specific (doc 05 cites real-estate 60–90d); calibrate per playbook once GG's decay pattern is observed.
- **aeo-audit base check weights** (AI-crawler access + schema highest) — revisit once we can see which checks actually move citations for a real client at Gate 1a.
- (Not data-dependent, no revisit expected: schema-generation date-match exemption + price-candidate policy; compliance TCPA presence-scope + alcohol-jurisdiction warn; brand-kit contrast pairs. Auto-rollback thresholds are already per-client-configurable — tuned in Phase 1.2, not here.)

## Doc-silent assumptions from 0.2 — RATIFIED 2026-07-07 (superseded by the block above; retained for the decision trail)

**Ratify at F1 (0.3, lead-backend-data-architect):**
1. `tenants.theme.font` sub-shape — doc 03 has singular `font`; doc 06 requires three faces. Library emits `font: {display, body, mono}`.
2. `tenants.theme.colors` key naming — snake_case (`surface_raised`) chosen per jsonb convention.
3. `AutomationLevel` lives as a local copy in aeo-audit `types.ts` — unify into the shared data-layer types at F1.
4. `SchemaBrandContext` identity gap (schema-generation `types.ts` ~467–479) — brand context shape must line up with the `brand_kits` schema.
5. Compliance registry + regex cache are module-level state — must be tenant/request-scoped when wired into the app (flag carried to the 1.x integration review).

**Ratify at F2 (0.4, operator sign-off):**
6. Positive/negative distinguishability policy: hue separation ≥ 30° OR mutual contrast ≥ 1.3:1, auto-separated by lightness (brand-kit `CONTRAST_REQUIREMENTS`).
7. Contrast pair-set policy incl. surfaceRaised pairs — being set by the design lead in 0.2 remediation; carries into the theming engine.

**Ratify at Freeze Gate 0 (operator):**
8. aeo-audit library-defined base check weights (AI-crawler access + schema highest at 12) — playbook schema has no per-check weights; single override point exists if F1 adds them.
9. aeo-audit refresh-window default 90 days (outer bound of doc 05's 60–90 real-estate example), overridable per audit.
10. aeo-audit review-velocity thresholds (hyper-local ≥4/30d recency ≤30d; semi-local ≥1/30d recency ≤60d).
11. schema-generation: date fields are sanity-validated but exempt from visible-text matching (rendered dates can't be deterministically matched) — needs a line in the skill doc.
12. schema-generation: Review/AggregateRating supported as standalone roots (doc 02 profiles list them standalone; SKILL.md showed them only nested).
13. compliance: TCPA check verifies consent-language presence, not legal sufficiency of the consent flow — explicit boundary for compliance-review.
14. compliance: state-by-state alcohol-promotion rules have no matrix in doc 02 → warn with jurisdiction note, never silent pass.

## Resolved escalations (operator resolutions, 2026-07-07)

1. **Phase 1 sequencing (doc 00 §5 vs doc 07):** RESOLVED — **doc 07 is authoritative** for Phase 1 sequencing. Doc 00 §5's ordering is superseded where they differ.
2. **"content pipeline" owner:** RESOLVED — added a 13th agent, **`content-production-engineer`**, owning M8, M9, M11, M12, M15. It generates content; `content-quality` and `compliance-review` remain the independent reviewers — a producing agent never approves its own output. Doc 01 (roster + agent block), doc 05 (module map), doc 07 (1.5/1.7 owners), and `.claude/agents/` updated.
3. **Skills as definitions vs code:** RESOLVED — skills stay as Claude Code definitions; the isolation-tested implementation library lands in 0.2. **The 0.2 gate (Code Review + QA) tests the underlying library, not just the definition.**
4. **F2 design direction encoding:** CONFIRMED as done. **F2 freeze gate:** RESOLVED — Design Review alone is insufficient; the F2 freeze requires **operator sign-off** as independent reviewer. Doc 01, doc 07 (0.4 + Freeze Gate 0), and CLAUDE.md updated.
