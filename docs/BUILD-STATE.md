# BUILD-STATE — live task board
### Maintained by: lead-architect-orchestrator (state) + documentation (freeze log)

> The Orchestrator drives from `07-build-sequence-and-execution-checklist.md`. This file records where the build actually is. Do not cross a 🔒 gate marked ⏸ below.

## Current stage: **PHASE 1 MODULE BUILD COMPLETE (M1–M19 all built + gated, 2026-07-09) — foundation frozen; operator live. Every module behind fail-closed deferred vendor ports; NOTHING wired to live vendors or publishing to a live site yet. NEXT is 🔒 GATE 1a: prove the ENTIRE loop end-to-end on real estate / GG client zero (onboard → plan → audit → produce → publish w/ rollback → track → report) with REAL vendor accounts + a 4–8wk citation-measurement window — an OPERATOR + real-data milestone, not a build task. Before Gate 1a can start, the recorded WIRING PRECONDITIONS must clear (write-methods per-property serialization + DNS-rebinding; content/schema/review-response compliance structured-signal threading; platform-gate ruleset extension; approval-transition lifecycle w/ passed-verdict + send-back state; pick-one-scan-source + M2 kind-filter; vendor signups). SHIPPED & gated (2026-07-09): real-data + plan-regen slices · 1.3 write methods (8/8) · 1.4 intelligence M2–M6 · 1.5 brand-production M7–M10 · 1.6 local+reviews M14/M15 · 1.7 social/PR M11/M12 · 1.8 measurement M16/M17 · 1.9 resource center M18 · 1.10 client dashboard M19. · 1.7 social/PR M11/M12 · 1.9 resource center M18 (all gated PASS). REMAINING for Phase 1: 1.10 client dashboard (M19) → then 🔒 GATE 1a (prove the full loop on real estate / GG client zero). Standing preconditions before live/regulated exposure (all recorded in gate records): write-methods wiring + crawler DNS-rebinding; vertical-aware ensurePlan supersede before a 2nd vertical; pick one scan source (M2 vs M6) + M2/M14 kind-filter before wiring scans into generatePlan; the content/schema/review-response compliance-wiring + structured-signal preconditions before regulated-vertical publish/send. · **UI-PHASE (operator-directed) now IN PROGRESS ahead of Gate-1a wiring — see the UI-phase note below.****

> **UI-phase pivot (2026-07-09 → 2026-07-10, operator-directed):** ahead of Gate-1a vendor wiring, the operator directed a UI-FIRST build — stand up the full operator page suite + studios launch-ready before connecting live vendors (local-prototype-first, standing rule 9). **Wave 1 SHIPPED (commit 321d6d4): operator app shell + IA + full route skeleton (~18 routes) — Code Review PASS (0 blockers/majors; no cross-tenant/cross-client/priv-esc path on any new route; 2546 tests; env-less build clean) + Design Review PASS (foundation; IA / role-split shell / token-driven re-skin approved; 4 Majors + minors queued for remediation).** A read-only UX/functional gap AUDIT (2026-07-10, lead-ui-ux-designer) then spec'd all 28 routes against a "completes the user's task end-to-end, handles every state honestly, never dead-ends" bar. **Headline finding: the Gate-1a loop is OPEN-CIRCUIT at FOUR points** — (1) **no human verdict layer exists** (nothing can be approved: NO code writes the `quality_review`/`compliance_review` columns the frozen `content_items` CHECK demands; no `needs_revision`/send-back status; `ChangeManager.apply/rollback` have no server-action or UI seam); (2) **onboarding drops the collected property URL/platform** (no code path writes `properties`), so no intelligence run-action has a target; (3) **content generation refuses without a brand kit and there is NO kit-ingest UI**; (4) **named competitors have no schema home** (both share-of-voice callers pass `[]`). Prioritized deep-build backlog delivered to operator to set order: **P0 governance-critical** (approval-lifecycle R3 → properties-persist → Review&Approvals deep → Brand-Kit ingest → Site-Changes diff/rollback → Connections page → run-triggers) → **P1 daily-driver** (Content Studio v1 → Plan-tab deep → Audit fix-list + Visibility per-prompt table → competitors home+M4 surface → Alerts acknowledge+top-bar badge → cross-cutting batch) → **P2 vendor/growth-gated** (Local/Reviews/PR detail → Measurement rollup → Resource Center → Media Studio → Settings theme/team → command palette → M19 export). **Wave 1 honesty+polish remediation SHIPPED & DOUBLE-GATED (2026-07-10) — Code Review PASS (+ follow-up CONFIRM) · Design Review REJECT (2 copy Majors: global "Dark glow" toggle label; "deep build" jargon in site-changes) → fixed same-touch → PASS ("this is the standard").** Landed: 3 honesty bugs (phantom 'Diff: Ready' pill → honest inert dash + sr-only; 'Total tasks' relabel; dashboard 'Coming online' → jargon-free 'Your intelligence signals' with live links); tenant-UUID→agency-name (RLS-verified own-tenant read, fail-closed); global Light/Dark toggle (preview page explicitly owns its "Dark glow" label); tab prefix-active + overflow fade + scrollIntoView (compare-before-set, dual ResizeObserver); (app)+(workspace) loading/error/not-found routes (client 404 relocated to the [clientId] boundary per CR Major — a (workspace)-level file can't catch its own layout's throws); EmptyState/PendingState action slots; truncation labels neutralized + review-queue exact HEAD counts w/ honest "N+" lower-bound pill on count-read failure; alert/queue rows → type-relevant workspace tabs; onboarding operator-guard (verified no static bake-out); viewer-redirect symmetry (no existence oracle — equality check before any read). CR verified: no service-role usage, no cross-tenant path, no open redirect, env-less + stub-env builds green, 2546 tests. Carried (non-gating): mode-toggle promotion out of preview namespace; root-page M-codes → jargon-sweep ticket; platform_owner short-id fallback (by design); root app/not-found.tsx for unmatched URLs; chrome-stripped "view as client" REQUIRED before first real client_viewer demo; tab-grouping strategy decision before an 11th tab; client search → P1/P2 command palette. **TWO GOVERNANCE ESCALATIONS raised to operator: (A)** background-execution architecture for long crawls — **RESOLVED 2026-07-10**: operator directed "recommend the correct, thorough way"; Orchestrator **RATIFIED the runs-queue architecture WITH BINDING AMENDMENTS** (see the 2026-07-10 architecture-ruling record below); **(B)** P0/P1 items require **POST-FREEZE schema additions** — the governed batch is now **SIX items** (`properties` persistence, `content_items.title`, `needs_revision` status, competitors home, reviews home, + the ruled `runs` table; properties must land with-or-before runs) — all via the governed path (Orchestrator + Code Review sign-off) per rule 1, NOT ad-hoc.

> **Provisioning note (2026-07-08):** the frozen F1 foundation schema (migrations 0001–0006) was applied to the operator's live Supabase project via the SQL Editor (the remote build environment's network policy blocks direct DB access, so the operator ran a combined script). Verified live: all 13 public tables present with `relrowsecurity` AND `relforcerowsecurity` = true. App-runtime connection secrets (`NEXT_PUBLIC_SUPABASE_URL`, anon key, etc.) to be wired when the first data-backed screen is built. Network policy note: this environment blocks outbound to Supabase/Vercel/GitHub API; allowlisting `*.supabase.co` would let the orchestrator manage the DB directly (operator/admin change).

> **Provisioning note 2 (2026-07-09) — auth layer LIVE:** with `api.supabase.com` allowlisted and an operator-supplied short-lived access token, the orchestrator provisioned the live project directly via the Supabase Management API (per `ops/environments.md` §Authentication): (1) migrations **0007 + 0008 applied** (verified: `auth_hooks.custom_access_token_hook` + `app.user_role` exist); (2) **Custom Access Token hook ENABLED** via auth config (`pg-functions://postgres/auth_hooks/custom_access_token_hook`); (3) operator auth user created (email confirmed); (4) **tenant #1 "The Design Lab"** created + operator wired as `agency_admin` in `tenant_users`. **End-to-end verified:** a real password sign-in against the live project returned a JWT carrying `tenant_id` + `user_role=agency_admin` with reserved `role=authenticated` — the hook mints claims exactly per contract §2/§12. The operator was instructed to revoke the access token immediately after. Remaining operator-local steps: `.env.local` (URL + anon key), `npm run dev`, sign in, change temp password.

| Step | Item | Owner | Gate | Status |
|---|---|---|---|---|
| 0.0 | Agent team + 4 skills operationalized (doc 01) | operator + orchestrator | — | ✅ Done (2026-07-07) — 12 agents in `.claude/agents/`, 4 skills in `.claude/skills/` |
| 0.1 | Project scaffold (Next.js + Supabase + Vercel, CI/CD, staging/prod, secrets, CF Workers readiness) | devops-deployment | Code Review | ✅ Done (2026-07-07) — Code Review **PASS** (see gate record below); operator-side provisioning steps listed in `ops/environments.md` |
| 0.2 | 4 skills implemented + tested in isolation (gate tests the LIBRARY, per resolution 3) | build agents | Code Review + QA | ✅ Done (2026-07-07) — gate CLOSED after remediation + green re-verification (see gate record) |
| 0.3 | Multi-tenant data model (F1) | lead-backend-data-architect | Code Review (security+isolation) + QA isolation suite | ✅ Done (2026-07-07) — **GATE CLOSED**, doc 03 §7 criteria 1–5 all MET (see gate record); freeze declaration awaits operator at Freeze Gate 0 |
| 0.4 | Design system + theming (F2) | lead-ui-ux-designer | Design Review + **operator sign-off** (resolution 4b) | ✅ Done — Design Review passed, B1 blocker fixed + re-review CLEARED (369 tests), **operator signed off 2026-07-08. FROZEN.** |
| 🔒 | **FREEZE GATE 0** — foundation frozen | orchestrator | all of 0.2–0.4 passed; F1 + F2 presented to operator | ✅ **PASSED 2026-07-08** — operator signed off both F1 and F2 after a local walkthrough. Foundation FROZEN; changes now require Orchestrator + Code Review sign-off. |
| 1.1 | Industry Playbook Engine (M1) — build first | aeo-seo-logic-engineer | Code Review + Content Quality | ⏭️ Next up — Phase 1 open |
| 1.x | Remaining Phase 1 feature work | (per doc 07) | (per doc 07) | ▶️ Unblocked |
| 1.1 | Industry Playbook Engine (M1) + onboarding flow | aeo-seo-logic-engineer (+frontend/designer) | Code Review + Content Quality | ✅ Done (2026-07-08) — both gates PASS; M1b deferred to M18-infra availability (see gate record) |
| 1.2 | Change-management layer + provider-agnostic connector interfaces | integrations-engineer | Code Review (rollback safety) + QA (rollback suite) | ✅ Done (2026-07-08) — BOTH gates PASS (0 blockers); all findings remediated (see gate record). 651 unit + 385 isolation green. |
| 1.3 | Auto-fix write methods (WordPress→Webflow→Wix→CF edge worker) implementing WriteMethodAdapter | integrations-engineer | Code Review + QA (rollback per method) | ✅ **DONE (2026-07-09) — all four methods, both gates PASS each.** Adapters library-complete; NOT yet wired to live properties (ticket ii — carries HARD preconditions: per-property construction AND per-property write serialization w/ proof test, + first-live-connect canaries per method). |
| 🔒 | **GATE 1a** — single-vertical validation (real estate, GG as client zero, full loop end-to-end) | orchestrator | loop proven + case-study metrics | 🚫 Not reached |
| 🔒 | **GATE 1** — Phase 1 ships multi-vertical | orchestrator | validated loop across seed verticals + M1b active | 🚫 Not reached |

## Post-freeze foundation changes (require Orchestrator + Code Review sign-off — CLAUDE.md rule 1)

**2026-07-09 · schema-generation skill: error-gate fabricated aggregate review counts/ratings · Orchestrator AUTHORIZED · Code Review → PASS · Compliance → PASS (scoped) (freeze-log sign-off)**
Trigger: building M10 (Schema generation) surfaced a Code Review BLOCKER — the frozen skill treated aggregate `reviewCount`/`ratingCount` as pass-through (never matched against visible text, never even surfaced to the human approver) and `ratingValue` as warning-only, so a page with ZERO visible reviews could emit `AggregateRating{ratingValue:4.9, reviewCount:5123}` and pass the "ready" gate — a Google manual-action risk AND FTC fake-review (16 CFR Part 465) exposure. Governing basis: doc 05 M10 "schema must match visible page text exactly." **Orchestrator disposition: tighten now (not defer).** `internal.ts buildAggregateRating` (shared by Product/Restaurant/standalone AggregateRating): `reviewCount`+`ratingCount` registered as number-kind ERROR claims, `ratingValue` promoted warning→error. `text-match.ts`: new `thousandsTolerantCore` adds thousands-separator tolerance (so `reviewCount:5123` matches a page rendering "5,123 reviews") with tightened digit-group boundaries proven not to over-match (`5,123,999`/`5,123.4`/comma-space-list all correctly reject) — Code Review ran an 18-case adversarial matrix, 0 bypasses, and it incidentally closed a latent pre-existing boundary bug. Strictly additive: non-review matchers byte-unchanged, existing rating fixtures still ready. Skill 0.2 isolation 85 green. **Individual per-review `buildReview` rating left pass-through** (deliberate; ratified conditionally by Compliance — the review BODY is error-gated so no phantom review can be emitted; the residual per-review star accuracy is bounded and needs a glyph/word-aware matcher as a follow-up, and the rating must be source-bound to the same genuine review record — holds while review data originates from M15 real feeds). **Signed off (Orchestrator + Code Review + Compliance-scoped).** Does not re-freeze.

**2026-07-09 · F2 brand-kit skill: extend the B1 CSS-injection defense to the typography scale · Orchestrator AUTHORIZED · Code Review → PASS (freeze-log sign-off)**
Trigger: building M7 (Brand Kit engine) surfaced a B1 GAP that Code Review caught as a Blocker — `typography.scale` (the `--text-${key}` custom-property names + `size`/`lineHeight`/`weight` step values) was the FIRST caller-controlled free-text token path to reach the skill's emitted CSS UNVALIDATED (B1's original premise was "font stacks are the only free-text token value"). A within-cap hostile scale injected via the skill's own serializer (reviewer PoC proved both value- and key-injection). Not live-exploitable in the shipped app (no built consumer serializes a per-client kit's scale — the theme engine drops it), but a STORED injection persisted into an immutable kit that would detonate when M8/M11/white-label serialize it. **Orchestrator disposition: close the class at the EMITTING layer** (consistent with the SSRF-guard and shared-refuse-helper dispositions). New `src/lib/skills/brand-kit/type-scale.ts` (mirrors `font-stack.ts`, throw-never-strip) wired into `resolveTypography` — the one authoritative resolve seam for `buildBrandKit` AND `reviseKit` AND the M7 wrapper. Strict ASCII whitelist grammars (keys `^[a-z0-9]([a-z0-9-]*[a-z0-9])?$`≤48; size digits+dot+closed-unit-set≤32; lineHeight same unit-optional; weight finite number [1,1000] — closes the `String(weight)` raw-emit hole); predicates exported so M7's defense-in-depth seam reuses one grammar (no drift). **Change is strictly additive gating: valid scales byte-unchanged (Signal scale returns same reference; `serialize.ts` exact `--text-*` output still pinned), no other skill behavior altered, the empty-scale throw preserved verbatim.** Re-review attacked newline/CR anchoring, backslash/CSS-unicode escapes, comment sequences, `url()`/`expression()`, unit-position injection, overlong payloads, and the NaN/Infinity/negative/string weight vectors — 0 bypasses; both PoC payloads rejected at both layers; `reviseKit` confirmed authoritative (base untouched on throw). Skill 0.2 isolation suite 122 green (+ new `type-scale.test.ts`); full suite 2029 green. Threat-model comment in `font-stack.ts` corrected to name the scale as gated. **Signed off (Orchestrator + Code Review).** Does not re-freeze F2.

**2026-07-08 · F2 token-system extension: add a SECONDARY + WARM accent · Orchestrator AUTHORIZED · Code Review → PASS (0 blockers, 0 majors, 1 minor)**
Code Review CLOSED the change: confirmed truly additive (7 frozen tokens byte-unchanged; theming engine, 5 moments, component library untouched), B1 CSS-injection defense intact for the new token paths (14 hostile `accent_secondary`/`accent_warm` payloads — brace-breakout, `</style>`, `url()`, `expression()` — all caught by the hex gate → Signal fallback), accessibility gate correctly applied (Alachua `#f4a200`→`#c18000` real + reported; Core Orange `#fc4c14` genuinely at ~3.14:1, gated not waved through), backward-compatible (pre-existing tenant rows without the fields still resolve via Signal default). Minor (stale `serialize.ts` header comment) fixed. **The two-accent extension is signed off.** Note: this does NOT re-freeze F2 — the operator's brand look itself is still a first pass awaiting operator reaction; a re-freeze of the operator theme happens only on operator sign-off.
Original authorization:
Reason: the operator's real brand (tenant #1, `docs/design/operator-brand.md`) has **two** headline colors — Core Blue `#1B2FCE` (primary) and Core Orange `#FC4C14` (secondary/energy) — plus Alachua `#F4A200` (warm) and Dark Blue `#0A1C46`. The frozen F2 token set carries a single `--accent`. To represent the brand faithfully, the token system is extended **additively**: add `--accent-secondary` and `--accent-warm` to `ColorTokens`/`DesignTokenSet` + `buildBrandKit` (accessibility-gated like the existing accent) + `toCssVariables`. The existing 7 tokens, the theming engine, the 5 moments, and the component library are unchanged; this is purely additive. Owner: `lead-ui-ux-designer`. **Not "done" until Code Review signs off** (security + no-regression on the frozen brand-kit skill) — this is the sanctioned controlled-change path, not a re-opening of the freeze. Reconciliation: the operator's brand guide is authoritative; the earlier Webflow-blue proxy (`#146EF5`) is discarded.

## Gate records

**2026-07-10 · PLAN TAB DEEP BUILD SHIPPED (P1 — the operator's daily home) · Code Review → PASS · Design Review → REJECT (3 Majors) → fixed → PASS · Orchestrator: `done` status AUTHORIZED (0013 next) + bounded ai_draft manual moves AUTHORIZED**
Ships: enriched task cards (impact band reusing the fix-list tones; module→human labels — no M-codes; effort/channel/source; native <details> full description; priorityScore drives order, never renders) · **priority-stable ordering** (Design M1: all of a plan's tasks share one bulk-insert created_at — a total tie that reshuffled on every refresh; now payload->priorityScore DESC nullsFirst:false + id tiebreak, "highest priority first" copy) · status filter (house recipe, whitelisted, exact HEAD counts, plan-id-scoped reads — twin-plan union structurally impossible) · **manual status control** via thin CAS'd `updateTaskStatus` (human_only todo⇄in_progress ONLY; automation_level + source-status as CAS predicates; approved/published/in_review/reverted structurally unreachable pre-DB, test-pinned; no oracle) · **total `statusNote` disclosure in every task's detail** (Design M2: the completion-gap note only rendered in unreachable states; now: machine-owned / pipeline-owned / "marking complete is coming" honestly per level) · in-workspace generate control (reuses the frozen action + dashboard outcome mapper; lives in the EmptyState action slot — Design M3: generate-variant structurally implies zero tasks; dormant-vertical + non-admin branches never invite an absent button; regeneration-policy disclosure line) · shared `makeAnnouncer()` factory extracted (the live-region pattern had reached THREE copies — dashboard/review-queue/plan now consume one source, wrapper-only diffs, byte-equivalent behavior). 2782 tests (+26 net); env-less clean.
**ORCHESTRATOR RULINGS (logged per instruction):** (1) **`done` task status AUTHORIZED** — governed path, migration 0013: `tasks_status_allowed` gains 'done' + a narrow structural CHECK (`status<>'done' OR automation_level='human_only'` — the honest word can never bypass pipeline terminals; approved/published stay load-bearing audit vocabulary); done→in_progress reopen permitted (work-tracking, not a verdict); the thin action extends to in_progress⇄done for human_only only; gates = CR + QA blessing tests (done rejected for ai_draft/auto on INSERT and UPDATE) + contracts-doc sync. (2) **Manual todo⇄in_progress on ai_draft tasks AUTHORIZED, bounded** (no pipeline writer exists — frozen-at-todo would be dishonest); pipeline words stay structurally unreachable; done structurally excluded for ai_draft; auto stays immovable; **wiring-time condition recorded**: the future pipeline→task block must define manual-vs-pipeline reconciliation at its own gate. (3) Pending-list disposition: governed items queue for Orchestrator sequencing at the UI-phase pause; ⚑ ratify set consolidates into the pre-Gate-1a package; **decay-persistence first off the bench** (only item gating an already-ruled capability).
**Carried:** disabled-focus-drop = recorded app-wide pattern (central fix tracked); the 0013 slice implements ruling 1+2 next.

**2026-07-10 · COMPETITORS PANEL + SHARE-OF-VOICE THREADING SHIPPED (P1 — SOV becomes real) · Code Review → PASS (1 pre-merge finding) · Design Review → REJECT (3 Majors) → fixed → PASS (all rulings upheld)**
The 0010 competitors table gets its UI and BOTH share-of-voice callers stop passing `[]`. Ships: **competitors panel on the Visibility tab** (add w/ verbatim validator refusals BY CALLING the landed contract; delete via the landed+tested removeCompetitor — Design RULED the affordance acceptable (internal config row, rule 4 inapplicable, two-step confirm); NO edit (no landed action — correction = remove+re-add, named in staff copy; VIEWER copy split APPROVED: plain fact, never instructions the caller can't take); cap-10 honesty w/ imported constant; managed focus incl. the stale-confirmingId hazard) + **SOV threading**: visibility page + the sanctioned M19 load.ts thin edit (net diff SHRANK — read chained inside Promise.all) pass real CompetitorRefs; name-only competitors flow through scoring as honest labelled 0% slices (verified end-to-end, test-pinned). **The gate catches:** (1) BOTH reviewers converged — the "Waiting on tracking" footer was unconditional AND factually wrong post-run (SOV recomputes against the latest STORED run at read time; an added competitor appears on next render) — now `hasRun`-conditional w/ the truth post-run; SOV-read-failed conservatively shows pre-run copy (ACCEPTED tradeoff: never claim a run you couldn't read); (2) a failed competitors read conflated with "no competitors" at both call sites — now routes SOV to its FAILED state ("a read blip must never render as 'no competitors set'", commented at both); (3) **hold-Enter defeated the two-step delete confirm** (autoFocus on Confirm + key repeat = one sustained keypress deletes) — autoFocus moved to Cancel. 2756 tests (+4); env-less clean.
**Carried:** frozen SOV chart density notes (11-bar height, long-name clipping, "This client" key edge — post-freeze pass w/ sign-off if wanted); editCompetitor action (the 0010 update policy is unconsumed — governed addition if editing is wanted); listCompetitors action unused by UI (valid contract, kept).

**2026-07-10 · ALERTS ACKNOWLEDGE LOOP + TOP-BAR BADGE SHIPPED (P1) · Code Review → PASS · Design Review → REJECT (3 small Majors) → fixed → PASS (no conditions)**
Closes the audit's "acknowledged alerts vanish irrecoverably / no acknowledge exists" finding. Ships: thin sanctioned `acknowledgeAlert` + bulk `acknowledgeAlerts` actions (writer floor mirrors alerts_update RLS; claims tenant; UUID clamp; CAS acknowledged=false→true, 0 rows = honest already_acknowledged, NO existence oracle incl. through the bulk count — RLS filters foreign ids before counting; bulk bounded to caller-visible ids, deduped, capped at ACTIVE_ALERTS_MAX imported not mirrored; **no un-acknowledge path exists structurally** — would corrupt the M5 fingerprint self-heal, which was verified fed correctly: the flip drops the row from readOpenFingerprints so standing conditions re-alert). Alerts page: per-row acknowledge (icon-only below sm — the narrow-row crush fix; labeled from sm up; one a11y-tree control at a time), acknowledge-all-visible, deep-linkable **Acknowledged history view** (?view= whitelisted; "flagged {date}" phrasing — the schema can't provide an acknowledge date; attribution-NEUTRAL copy — Design Major: "Alerts you've acknowledged" implied per-operator attribution a tenant-wide view without acknowledged_by can't back, fixed with a comment pinning why), exact HEAD counts per tab (muted tabular-nums, never a faked 0), cap note gated on count>limit, **one tab grammar in the product again** (ViewTab now carries the workspace-tabs house recipe — Design Major: it had invented a second). Top bar: quiet bell badge (accent not negative — "attention" is the honest claim; renders only on known positive count; 9+ cap w/ real count in aria-label; null/0/env-unset → bare bell, never fake zero; staff branch only — client_viewer never triggers the read; parallel with tenantName). 2752 tests (+11); env-less clean.
**Carried:** ⚑ `acknowledged_by`/`acknowledged_at` attribution columns = governed post-freeze schema decision (joins tenant_users display-name, decay persistence, reviews home on the Orchestrator's pending list); partial-unique fingerprint index flag (pre-existing, more pertinent now); if a third routed tab bar appears, extract a shared server-safe tab primitive (second copy of the recipe is the limit); desktop pending width-jump + panel title/description echo (nits, declined).

**2026-07-10 · AUDIT FIX-LIST + VISIBILITY PER-PROMPT TABLE SHIPPED (P1 — "the moat made visible") · Code Review → PASS · Design Review → PASS (both clean first try — 0 blockers/majors)**
Renders the persisted-but-invisible intelligence. **(A) The fix list** (doc 05: "the fix list IS the product"): the latest audit's priority-ordered fixes — impact pill (engine's real critical/high/medium/low), title/detail (verified interface-voice at the check-module source), impact estimate, target URLs (plain text, first 4 + "+N more", NEVER href); internal codes (checkId/module/priorityScore/automationLevel) dropped AT THE PARSE BOUNDARY (structurally absent from the view type, test-pinned); storage order preserved (no UI re-sort); "recommendations, not auto-fixes" caption per rules 4/5; count reconciliation ("Showing X of Y — Z couldn't be displayed") + honest corruption state; native <details> expansion (no JS); **the fix panel is keyed to the SAME audit id as the score above it — the two can never describe different runs**. **(B) The per-prompt table**: prompt × engine × Cited/Not-cited × position × sentiment × cited source from the latest run; ABSENT ≠ ZERO taught inline ("a dash means that signal wasn't captured, not zero"); unknown vendor sentiment verbatim-muted; 50-group bound + honest truncation; overflow contained; NO run affordance (visibility stays vendor-gated per A5). Engine additions PROVABLY additive (CR: every deletion in the diff is an import rewrite; the load-bearing M19/score reads byte-identical; `getLatestRunResults` deliberately separate column-set; `readLatestAuditFixes` RLS + defense-in-depth, PK-bounded; `parseStoredFixes` throw-free under hostile jsonb, honest deltas, +9 tests). 2741 tests; env-less clean.
**Carried minors (both reviewers, at-discretion):** sr-only dash hardening (aria-label-on-span); "source cited otherwise" → neutral wording; warning icon on the couldn't-display state; non-array `fixes` should not render as clean-pass (distinguish drift from empty — the one honesty-class carry); th scope=col; fix-list total render cap (parity w/ the prompt table's bound — same-tenant-writer large-jsonb scenario); fixes-panel placement promotion consideration; readLatestAuditFixes query-shape test parity; engine impactEstimate "High — " duplication (engine-copy question for aeo-seo owner).

**2026-07-10 · CONTENT STUDIO V1 SHIPPED (first P1 slice — the daily production surface) · Code Review → PASS (0 blockers/majors) · Design Review → REJECT (2 narrow Majors) → fixed → PASS ("a strong deliverable")**
The placeholder is gone; `/content-studio` is the real brief→pipeline surface, fully built for the not-connected era (vendor gaps are DESIGNED states linking to /connections — the studio lights up with zero code change at wiring). Ships: **brief form** (client selector — kit-holding clients eligible, kit-less listed honestly under "Needs a brand kit first" w/ real ingest CTAs; type cards imported from GENERATABLE_CONTENT_TYPES; topic + optional grounding-facts "one per line" in plain language "so it can't invent facts"; NO title field — the create contract takes none, generator produces the working title; flagged not faked, ruling ACCEPTED) calling `createContentDraft` contract-exact; outcome mapping: `generation_unavailable` → the flagship designed state (Design MAJ-1 fixed: NO false "it'll produce when connected" queue implication — "your inputs are kept right here… press Create draft again", mechanically true + comment-pinned), `no_brand_kit` → kit CTA, engine refusals verbatim; **grounding-clamp honesty** (CR catch): live N/100 counter + inline alert that BLOCKS submit over-limit — silent server truncation unreachable from the form; clamps hoisted to the pure types module (sanctioned value-identical engine edit — single source, drift dead). **Pipeline board** (single-client, ruling ACCEPTED for v1): four REAL lanes — Draft (w/ the M9 "Run authenticity check" advance action, production-framed never a decision, own designed vendor-deferred state) → In review / Sent back (bound note summaries, queue-identical rule) linking INTO the review-queue detail (one decision surface verified structurally: zero verdict-action imports) → Approved (honest publish-not-wired text, NO publish affordance); exact HEAD counts + truncation honesty; independent failure domains; every outcome announces (MAJ-2 fixed: role=status on all designed states both islands). CR verified: 10 bounded parallel queries, UUID-validated params, no existence oracle, verdict jsonb narrowed server-side, islands receive only scalars. 2732 tests; env-less ƒ dynamic fail-closed.
**Carried (P1 backlog, greppable in page.tsx + studio-reads.ts):** STUDIO_TITLE_AT_BRIEF_GAP + STUDIO_CONTENT_READ_GAP (backend, governed path); choice-card primitive w/ radio semantics (F2-adjacent — the aria-pressed pattern is now in 2 surfaces, don't let it reach 3); selector optgroups; lane empty-title polish; helper aria-describedby completion; kit-eligibility should read `locked` explicitly (consistency-by-precedent today); unbounded directory reads → canonical projection; per-client in-flight counts on directory rows; M9-era authenticity chip on Draft/In-review cards (doc 06 §5) when the vendor wires; uniform focus-restore polish (app-wide).

**2026-07-10 · CONNECTIONS STATUS BOARD + TONE CONSOLIDATION SHIPPED — ⇒ THE P0 GOVERNANCE-CRITICAL BACKLOG IS COMPLETE · Code Review → PASS (0 blockers/majors; the static-guard question verified SAFE in all three build configs) · Design Review → REJECT (1 Blocker: a FALSE capability claim — GBP "rank tracking" vs the shipped READINESS assessment) → fixed → PASS**
Ships: **/connections** (operator page, nav FOOTER beside Settings — workspace-administration register, ruling approved): one honest row per vendor capability (Content & AI: Anthropic/humanizer/detectors · Intelligence: citation data · Measurement: GA4/GSC/call-tracking · Local & Reviews: GBP/review platforms · Distribution: social · Site connections → the shipped Properties panel, pill "Registration live" scoped to registration only), each row = plain-language name + a VERIFIED-TRUE "unlocks" sentence tied to a real shipped surface (every claim audited against the module code — the one false claim was the Blocker: connecting GBP turns on local-pack READINESS, NOT rank tracking, which is a separate deferred vendor per local/types.ts; fixed verbatim + header pointer so it can't regress) + structural "Not connected" muted pill + "setup arrives with vendor wiring" caption; NO fake buttons/fields/progress (verified structurally: zero handlers/forms/fetches/client islands); forward-compat VendorPort row shape (status union + reserved action) so real statuses land without redesign; dashboard citations card cross-links "What turns this on?". **Guard mechanic PROVEN in all three configs** (CR): env-provisioned builds render dynamic (the (app) layout session read hits cookies() → guardOperatorSurface per-request); env-less builds bake a 307→/login (fail closed — artifact inspected); hybrid misconfig = bricked-but-fail-closed; the mechanic + refactor hazard recorded in the page header. **Tone consolidation (the thrice-recurred AA class KILLED):** single source `src/components/tone.ts` (Design ruling: relocated out of the app layer; GOVERNANCE header pins it as app-utility over F2 tokens, NOT frozen F2), app-layer + review-queue re-export chains, 9 call sites across 7 files swapped, zero-visual-change PROVEN at the built-CSS level (each recipe emitted exactly once), zero remaining src/components→app reach-ins, and the contrast proof test now PARSES its expected share out of the shipped class (the proof follows the artifact). Copy honesty minors fixed (citation clause scoped to the prompt library; social clause to "approved in Review & Approvals, never auto-posted"). 2732 unit tests; env-less ○/stub-env ƒ both proven.
**P0 SCORECARD — ALL SEVEN COMPLETE (2026-07-10):** ① approval-transition lifecycle (R3) ✅ 0009 · ② properties persistence ✅ write-path + panel · ③ Review & Approvals deep build ✅ · ④ Brand Kits ingest+detail ✅ · ⑤ Site-Changes diff view ✅ (within ③; apply/rollback seam = integrations block) · ⑥ Connections page ✅ (per-client half + vendor board) · ⑦ run-trigger slice ✅ (queue + audit kind + UI). **NEXT: P1** — Content Studio v1 → Plan tab deep → Audit fix-list + Visibility per-prompt table → competitors home UI/SOV threading → Alerts acknowledge + top-bar badge → cross-cutting batch. Operator local walkthrough (rule 9) recommended before the P1 wave: onboard → brand kit → run audit → review queue, plus the A9 PostgREST empty-case canary.

**2026-07-10 · RUN-TRIGGER UI SHIPPED (P0 — the first operator-fired intelligence) · Code Review → PASS (0 blockers/majors) · Design Review → REJECT (1 Major: the recurring warm-text light-AA class, on the A5 stale signal) → fixed → PASS**
The Audit tab now runs scans: per-property "Run audit" → the landed enqueue action (refusals in product voice; no-property → EmptyState CTA to the Overview Properties panel — no dead end); **run status list from REAL rows only** (queued/running/succeeded/failed/canceled pills, year-bearing datetimes, target property); **A5/A6 binding conditions MET**: running rows expose heartbeat freshness via the exported `isHeartbeatStale` mirror ("Last progress Ns ago" / warm AA-fixed "No recent progress" for stale-not-yet-swept), honest attempt math ("Attempt N of 4" — verified against the 0-based column + cap-3 config + the 0012 structural cap), closed error codes → plain language w/ the retry clause keyed purely on attempts-vs-cap (honest to today's code-blind requeue; ⚑ revisit if the retryable-vs-terminal matrix is ratified); succeeded runs link to their audit-history anchor ONLY when the result is provably in the rendered set (no fabricated links); NO fabricated progress indication anywhere. **Cancel: queued rows only** via new sanctioned thin `cancel.ts` (mirrors enqueue: writer floor, claims tenant, UUID clamp, status-only CAS satisfying the 0012 cancel edge, honest `already_started` on 0 rows, NO existence oracle — foreign/nonexistent/started all indistinguishable; caller-neutral action copy w/ the UI owning the "we've refreshed" clause it actually executes). Calm freshness: 6s auto-refresh only while rows are active + tab visible + no action in flight; 1s ticker gated on reduced-motion with freshness still advancing via server resyncs (honest under reduced motion); hydration-safe (relative times client-only post-mount). Read-only roles see status, never buttons; the writer-only runs SELECT renders viewers an honest neutral empty state. CR verified: no server-only leakage into the client component, interval/ticker cleanup + no stacking, bounded reads, independent failure domains, props carry no tenant ids beyond need. 2732 unit tests; env-less build clean.
**Carried:** pre-mounted polite live-region primitive for positive notices (shared fix w/ the properties panel); tone.ts recipe consolidation (the warm/positive AA class has now recurred across THREE slices — priority for the next cross-cutting pass); same-minute duplicate cancel-label residual; idle no-op interval in hidden tabs (declined-cosmetic, commented).

**2026-07-10 · SCAN-QUEUE EXECUTION LAYER SHIPPED (the ruled A1–A9 architecture made real) — migration 0012 + processor/sweeper/kick/enqueue + the A7 DNS-rebinding closure · Code Review → REJECT (1 Blocker, live-demonstrated) → fixed → PASS · QA → FAIL (3 findings) → fixed → PASS (0-bypass spoof battery) · Orchestrator: A1 AMENDED (closed 3-function set) · A4 AMENDED ("enforced by the database" for runs) · audit-only scope CONFIRMED · undici dep ACKNOWLEDGED**
Ships: **0012** — `lease_next_run()` + `reap_orphaned_runs()` + `requeue_failed_runs()` (SECURITY DEFINER, search_path='', EXECUTE revoked from public/authenticated/anon, service_role only — **A1 formally amended: these three are a CLOSED SET; any fourth returns to the Orchestrator before drafting**) + **`app.runs_transition_guard()` BEFORE UPDATE trigger** (QA F2 posture correction, Orchestrator-authorized: legal edge set pinned IN THE DATABASE — queued→running lease-only; holder heartbeat/complete lease-fenced; running→failed('orphaned') reap-only; failed→queued requeue-only w/ attempts=OLD+1 exactly + structural cap 3 in-trigger; queued→canceled the one raw tenant edge; terminals have NO outbound edges; row identity tenant/client/kind/property immutable post-insert, requested_by →NULL only (FK SET NULL carve-out, two-step rewrite refused); caller discrimination = transaction-local GUC AND current_user=owner — **0-bypass proven**: hand-set GUC refused, claim-injected GUC inert, forged claims refused, service_role WITHOUT definer context refused BY THE TRIGGER even WITH granted table access, no public function PostgREST-executable; 0011's "transition legality as app-logic" header claim SUPERSEDED). **Processor** (`/api/runs/process`, app's first route handler): runtime=nodejs, POST-only, timing-safe secret, env-unset→503; lease via the sanctioned RPC then **ALL tenant data access through a per-run minted tenant-JWT RLS client** (tenant_id + user_role=operator; the service-role client touches ONLY the 3 RPCs, never a tenant table — probed); heartbeat 12s at the crawler's fetch seam; **zombie writes lease-FENCED** (the CR Blocker, demonstrated live as canceled→succeeded: completion/heartbeat now CAS on status='running' AND attempts token; 0 rows = lost lease → redacted stand-down, no retrofit — re-probed 0-rows on all three traces, trigger defends in depth behind the fence). **A7 closed**: undici (exact-pinned 8.7.0) custom-lookup dispatcher — every resolved address vetted via the egress guard fail-closed, the socket connects to the vetted address (TOCTOU collapsed; TLS servername intact; redirect:"error" + 15s timeout survive byte-for-byte); the long-carried DNS-rebinding precondition is DONE. **Sweeper** (`/api/runs/sweep`): orphan-reap (threshold maxDuration+60s, error_code='orphaned' distinct from engine failures) + capped-backoff requeue + **unconditional processor kick** (QA F1: a conditional kick stranded queued rows forever — proven, fixed, regression-pinned); hosted scheduling documented NON-OPTIONAL (Hobby once-daily = up-to-24h pickup, stated honestly). **Enqueue**: writer-floor, claims-sourced tenant, honest refusals (visibility→vendor_unavailable; monitor/decay/local/entity→unsupported_kind — **audit-only first pass CONFIRMED by Orchestrator**; decay has no persisted-artifact home → compute-and-discard prohibited), non-blocking kick. A9 holds: the whole loop runs under npm run dev, no cron. Budget invariant: one constant, budget=(60−10)s ≤ maxDuration−overhead, test-pinned incl. a source-reading pin on both routes' literals. **Final: 2727 unit + 530 isolation (62 queue tests incl. QA's spoof battery + exploit pins proven RED without the trigger), 5× flake-free, env-less build clean, 0001–0011 byte-untouched.**
**Per-kind adapter ownership (Orchestrator map):** M2/M5/M6/M14 kinds → aeo-seo-logic-engineer; **entity (M12) → content-production-engineer**; decay REQUIRES a persistence schema decision (governed path) before wiring; run-trigger UI offers only wired kinds. **⚑ Ratify at wiring review:** retryable-vs-terminal error_code matrix (CR recommendation: engine_error/orphaned retryable; misconfigured/crawl_refused terminal-immediate; budget_exhausted_total ≤1 retry). **Carried:** export `asLeasedRun` for a direct unit pin; PostgREST empty-case serialization canary at the A9 local walkthrough; run UI renders attempt counts + heartbeat freshness (A5/A6); processor 503-vs-401 disclosure trade-off + hosted chained-kick teardown recheck at hosting review.

**2026-07-10 · ONBOARDING PROPERTY WIRING + WORKSPACE PROPERTIES PANEL SHIPPED (P0; closes the batch's carried item c + the audit's dead-end bar) · Code Review → REJECT (1 Major) → fixed → PASS · Design Review → REJECT (3 Majors) → fixed → REJECT (2 panel Majors) → fixed → PASS**
The binding Orchestrator condition MET: the step-3 copy flipped to the truth IN THE SAME DIFF as the wiring. Ships: (a) **onboarding wiring** — the primary website (url+platform) persists via `createClientFromOnboarding.website` (singular per ruling; extras labeled honestly as workspace adds, never sent); same-key retry sends a byte-identical payload; client save can NEVER be lost to a property problem (probed on every path); `propertyWarning` soft-fail rendered as a calm warm notice; (b) **the promise mechanically cannot outrun persistability** — the round both reviewers converged on: `canAdvance` originally passed `mysite.com` under a "saves" badge while the server requires absolute http(s); fixed via shared pure `src/components/properties/website-url.ts` where the client gate IMPORTS AND CALLS the server's own `sanitizePropertyUrl` (drift structurally impossible; gate≡server property-tested over adversarial shapes) + idempotent https:// normalization with display=payload byte-identity pinned; badge reworded "Saves with this client" (designation, not receipt); (c) **finish-screen next steps** (audit A3e): real "Add their brand kit" CTA → the landed studio; honest inert "Connect their site — Not available yet" panel; plan reveal stays primary; (d) **NEW minimal workspace Properties panel** (Overview tab — deliberately NOT an 11th tab) consuming the landed createProperty/editProperty seam: RLS-scoped read w/ independent failure domain, list/add/edit (no delete per v1 ruling, no connect affordance), URLs plain-text-never-href (the logoUrl lesson held), server-truth rows (no optimism), router.refresh() + re-seed on unconfirmed round-trips so the recovery instruction is genuinely followable, want-flag focus management, AA-fixed positive/negative notice text, "Platform not recorded" fallback (no raw DB token can render). This panel makes the warning's "add it from the client's Properties" pointer TRUE — the dead-end both reviewers flagged is closed by existence, not rewording. Also: sanctioned character-level PROPERTY_WARNING apostrophe edit; save-outcome threading pinned (absent≠saved, warning-wins); removeProperty floor. 2652 unit tests (+17); env-less build clean.
**Carried (non-gating):** platform vocabulary relocation out of onboarding-copy to a neutral home; error/positive recipe consolidation (tone.ts class now inlined in several files); SEAM_UNREACHABLE copy micro-polish ("the list below" positional nit + async half-beat); theoretical stale-refresh race note (sub-second retry timing, self-corrects, loses nothing); per-client max-version aggregate for the kit library (backend-owned, already carried).

**2026-07-10 · REVIEW & APPROVALS STUDIO SHIPPED (P0 item 3 — the governance surface) · Code Review → PASS + CONFIRM (0 blockers/majors) · Design Review → REJECT (3 Majors) → fixed → REJECT (1 reviewer-mis-scoped Major) → fixed → PASS in full**
The page that makes "AI drafts, humans approve" a working human flow, driving the landed R3 lifecycle verbatim. Ships: **content review detail** (`/review-queue/content/[itemId]`: full body, title w/ honest "Untitled", humanization panel — "not run" ≠ failed ≠ 0, verbatim per-detector scores, neutral fallback for unmapped flags; both gate-verdict panels incl. the stale states "recorded against an earlier revision" AND "not bound to this revision" for malformed/missing hash — panel and approve-blocker always agree) · **decision controls** (record-quality/compliance verdicts w/ required-note-on-fail + per-gate epoch remount + inline "Recorded" echo; APPROVE enabled only when the exported `evaluateApprovalGate` says so — mirror-BY-INVOCATION, never re-implemented, server + DB CHECK re-enforce; send-back w/ required bounded reason; resubmit; every decision announced via the persistent aria-live region and followed by MANAGED FOCUS — status-keyed for branch-changing decisions, epoch-keyed per-recorder for verdicts) · **needs_revision rendering — the BINDING Orchestrator condition VERIFIED MET** (warm banner w/ every failing gate's note + datetime before the body; queue-row "Sent back:" summaries) · **site-change detail** (`/review-queue/changes/[changeId]`: stored-diff before/after, "Where this change lands" — client/property/page/locator from the ratified diff `target` (closing the named audit gap; typed from change-management, NOT the stale db.ts mirror); stuck-previewed anomaly banner (`previewed && applied_at != null` only); apply/rollback disabled handler-less in a Server Component — structurally inert; NO publish affordance anywhere) · **queue upgrades** (detail links, whitelisted client/kind/status filters — uuid/closed-enums only, filter state carried through row+back links, honest no-overlap state w/ Clear filters for contradictory combos, exact HEAD counts + "N+" lower-bound preserved). New read layer `detail-reads.ts` (RLS-scoped, no existence oracle, defensive verdict parsing, approver uuid never rendered — "Approved by you/a teammate · datetime"); shared pure `limits.ts` (sanctioned +3/−2 actions.ts edit, values identical); light-theme warm-text contrast fixed via the ErrorLine color-mix recipe (computed 4.8–5.3:1 AA). **Notable process point: the second Design REJECT was the reviewer catching ITS OWN mis-scoping** (verdict-record focus eviction via disabled-flip + remount — a path round 1 marked "unaffected"); fixed with the same want-flag/target-ref mechanism, disjoint from the M3 effect by construction. 2635 unit tests green; env-less build clean; NUL clean.
**Carried:** (i) **tenant_users display-name column** — post-freeze Orchestrator schema decision (interim "you/a teammate" copy RULED ACCEPTABLE); (ii) error-path focus restore (uniform, mitigated by role="alert", polish backlog); (iii) M19 "In revision" label CLOSED as ACCEPTED; (iv) change-approval/rollback server-action seam remains the separately-gated integrations block (per-property wiring preconditions unchanged); (v) content rows' precise item-detail links from ALERTS (not queue) when alert→item mapping lands.

**2026-07-10 · BRAND KITS STUDIO SHIPPED (P0 item 4) — ingest + detail + revise · Code Review → REJECT (1 Major) → fixed → PASS · Design Review → REJECT (4 Majors) → fixed → PASS**
UI-only build over the frozen M7 engine/skill (verified byte-untouched both rounds). Ships: ingest flow (client pick → palette/typography/voice/logo/likeness inputs → **read-only dry-run preview seam** (`_actions/preview.ts`, requireOperator, clamp-first, zero writes — exists because the frozen write actions drop the IngestionReport on refusal) → review step w/ **"What will lock" panel rendering the engine's post-merge resolved tokens** → explicit lock-confirm dialog, un-dismissable while the write is in flight) · structured contrast-refusal state (verbatim engine checks/adjustments, hedged headline, honest fallback for evidence-less refusals) · kit detail (swatches/typography/voice/provenance/version history — counts only, honestly) · revise flow (mode-specific blank-field copy: cleared color/font CARRIES FORWARD, logo blank REMOVES — every sentence verified true against the engine's deep-merge; version number never asserted pre-assignment) · list page → kit detail links, recency-fair scan + honest scan-cap note. **CR Major (fixed): the stored logoUrl rendered raw into `href` — the frozen clamp explicitly delegates scheme safety to the consuming renderer and this was the first renderer; now allow-list gated (WHATWG parse, http/https only, fail-closed), full hostile matrix re-probed (javascript:/data:/vbscript:/file:/blob:, case + whitespace mangling, protocol-relative) — all render as plain text w/ honest note.** DR Majors (fixed): revise blank-field semantics no longer lie + merged-result preview; in-flight dialog dismissal; a11y (aria-describedby, phase-change focus management, refusal role="alert"); refusal headline over-assertion + empty-report fallback. 2635 unit tests green; env-less build clean; both narrow re-probes PASS.
**Carried follow-ups:** (i) **M7 engine (post-freeze path, Orchestrator to authorize):** IngestionReport omits the distinguishability check's evidence (a distinguishability-only refusal carries no structured reasons); engine note strings say "the skill's default"/"Signal" (internal wording rendered verbatim per honesty rule — wording fix belongs to the skill owner); (ii) **backend read contract:** per-client max-version aggregate (distinct-on view/RPC) for the kit library (interim recency-scan is honest; note the updated_at tie-ordering residue for its design); (iii) **tenant_users display-name column** (post-freeze schema decision — also needed by Review&Approvals' "Approved by {name}"); (iv) polish: backToEdit focus restore, ListEditor per-input aria-labels, spacing tokens display when spacing authoring lands. Operator local-prototype review (rule 9) still applies before any hosting.

**2026-07-10 · GOVERNED POST-FREEZE SCHEMA BATCH LANDED — migrations 0009/0010/0011 + properties write-path + R3 approval lifecycle · Code Review → REJECT (1 Blocker) → remediated → PASS · QA → FAIL (same defect independently) → remediated → PASS (468/468) · Orchestrator ratified 5 escalations + REDIRECTED 1 + DIRECTED jsonb tightening**
The batch authorized under the 2026-07-10 scope record, drafted by lead-backend-data-architect. Ships: **0009** content_items `title` (≤200, no backfill, null→"Untitled"), `body_hash` BEFORE-trigger (pg_catalog.sha256 over `jsonb_build_object('title',title,'body',body)::text` — RATIFIED with binding condition **the trigger is the sole hash producer**; verdict actions copy the stored hash as read, never recompute), `needs_revision` status, **strengthened approval CHECK**, `approved_by/approved_at` + approver-present CHECK + composite FK `(tenant_id,approved_by)→tenant_users`; **0010** competitors (authorized shape exactly; cap-10 app-enforced ⚑); **0011** runs (A4 conditions; `runs_select` **writer-only** — RATIFIED deviation-by-restriction; error_code closed enum; `lease_next_run()` deliberately NOT drafted — belongs to the queue-infra block per A8); **properties write-path** (no migration — frozen shape sufficient): `ensureWebsiteProperty` on BOTH fresh + 23505-replay onboarding paths (divergent replay UPDATE-THROUGHs, never strands a stale URL — test-pinned), workspace create/edit seam, **no delete v1**, `connection_method` locked to `'none'` (Orchestrator REDIRECT: `'pr'` = the **Git/pull-request write method** (doc 04), NOT press-release — misreading scrubbed, hand-setting refused); **R3 lifecycle actions** (record-verdict ×2, approve, send-back w/ required bounded reason recorded as a failing verdict bound to body_hash — RATIFIED, no dedicated column; resubmit; demote-before-edit; publish NOT wired).
**The gate cycle that mattered:** Code Review and QA **independently** found the same Blocker — the strengthened CHECK failed OPEN on absent/JSON-null `body_hash` (Postgres CHECKs pass on NULL; plain `=` propagates NULL), proven live: hashless verdicts → approve accepted → approved body mutated with no re-review; QA showed reachability by any is_writer via PostgREST and pinned 3 RED regression tests. Orchestrator had in parallel DIRECTED boolean-type strictness. **Remediation (one move):** `is not null` presence conjuncts + jsonb **containment** — `quality_review @> jsonb_build_object('passed', true, 'body_hash', body_hash)` (ditto compliance; `humanization @> '{"passes":true}'` w/ schema_copy + human_only exemptions) — two-valued (never NULL), type-exact (string "true"/number 1/junk all reject via the named constraint), hash presence+equality in one predicate. **Re-probes:** CR PASS (original vectors + array/scalar verdicts, extra-keys subset-match intended, dup-key normalization, hash type-confusion — no new bypass; positive path + post-approval-mutation rejection intact; down-blocks re-proven schema-identical); QA PASS (3 RED flipped green with test files byte-identical in query+params+pattern; +3 new array-edge pinning tests; tripwire proven to catch a hypothetical policy-less table). Final: isolation **468/468** (was 399), unit **2635** (was 2546), typecheck/lint/env-less build clean.
**Carried (named preconditions/follow-ups):** (a) **publish-wiring block**: `'published'` is app-unreachable today but NOT DB-gated on change-management linkage — rule-4 linkage must be enforced when publish is wired (QA observation, joins the R3/publish precondition set); (b) reviews-home schema decision = named prerequisite of the review-platform vendor-wiring block (Orchestrator deferral); (c) onboarding UI wiring of the website property = immediate P0 frontend follow-up, and step-3's "nothing you enter on this step is saved yet" copy must flip **in the same change** (Design+Code review); (d) Review&Approvals UI must RENDER the send-back note(s) bound to needs_revision rows (Orchestrator binding condition); (e) CR minors: control-char stripping in competitors/properties validators (consistent-with-frozen-precedent, optional hardening); M19 "In revision" label copy → next Design Review; (f) ⚑ ratify: competitors cap-10; runs thresholds (A6) at queue-infra wiring. Contracts doc update (`docs/contracts/data-model.md`) = documentation agent, required before batch declared fully done.

**2026-07-10 · ARCHITECTURE RULING — background execution for long-running scans · escalated per rule 7 (docs 00–07 silent) → operator directed "recommend the correct, thorough way" → Orchestrator RATIFIED WITH BINDING AMENDMENTS**
**Ruled architecture: a Postgres-backed `runs` queue (single source of truth) + Node-runtime processor + kick-on-enqueue + scheduled sweep.** Enqueue = instant tenant-scoped insert (`queued`); processor = internal Node route handler leasing via `FOR UPDATE SKIP LOCKED`, heartbeating, executing the engine under its existing honest budget; sweeper retries strays w/ capped backoff and marks stale-heartbeat orphans `failed` (never stuck-running). Grounds: only alternative satisfying all four hard constraints — the **Node pin** (egress-guard's DNS-rebinding closure = undici custom lookup, Node-only; verified in `crawl/egress-guard.ts` + `audit/live-fetch.ts`), RLS on the state store, the honesty rule (real states, honest `budget_exhausted` truncation), zero new vendors pre-Gate-1a. Rejected: fire-and-forget (silent death = fake "running"), Supabase Edge Functions/Deno + CF Workers (forfeit the Node pin; worker stays on its doc-04 charter), dedicated worker now (recorded as the scale-up path consuming the SAME queue; chunked resumability via `progress` jsonb frontier is the recorded thoroughness follow-up). **SCOPE: read-only scans ONLY — a `runs` row must never vehicle a client-site write; writes stay behind change-management (rule 4).**
**Binding amendments:** **(A1) processor data-access model** — the cross-tenant lease is the ONLY service-role-class op permitted (recommended: one reviewed `SECURITY DEFINER lease_next_run()`, EXECUTE locked down, QA-isolation-exercised); ALL subsequent tenant reads/writes during execution go through an RLS-enforced client scoped to the leased run's tenant (per-run minted tenant-claims JWT or equivalent); any other service-role use on the run path = automatic Code Review reject; if per-run claims prove impractical, escalate back — no silent downgrade. **(A2) processor hardening** (app's first route handler): explicit `runtime="nodejs"` (CR verifies no edge runtime anywhere on the crawl path), timing-safe shared-secret auth, secret in env table + never logged, env-unset fails closed (no 500), POST-only, no request-derived run selection beyond the lease. **(A3)** crawl budget + route `maxDuration` derive from ONE config constant with invariant `budget <= maxDuration − overhead`; truncation via the crawler's honest path, never routinely via platform kill; plan dependency (Hobby/Pro windows, Hobby cron once-daily, pg_cron alternative) documented in `ops/environments.md`. **(A4) `runs` schema conditions**: doc 03 §3/§4 exactly (tenant_id indexed + client_id, RLS enabled AND forced, composite-FK tenant consistency, ratified write-floor); legal transitions pinned + tested (queued→running→succeeded|failed; queued→canceled; running→failed by sweep only; retry re-queues same row attempts+1 to cap; operator re-run = new row; terminal immutable); `error_code` closed enum (no raw error text/URLs); `progress` jsonb bounded + content-free; QA isolation suite extended. **(A5) state honesty**: UI renders only real table states; running exposes heartbeat freshness; stale-not-yet-swept renders "no recent progress"; cancel only on `queued` (CAS) until a real mid-run cancel exists; vendor-gated kinds (visibility) refuse at enqueue — no queued-forever rows. **(A6) ⚑ provisional thresholds** (ratify at wiring review): heartbeat ≤~15s between fetches; orphan = maxDuration+60s since last heartbeat; attempt cap 3 capped backoff; NOTE the crawl-bounds ratification (30/3/1.5MB/120s) is the real thoroughness lever — the queue makes raising them safe. **(A7) the DNS-rebinding socket-pinning closure LANDS WITH the processor** (the processor IS the live-fetch seam; owner aeo-seo-logic-engineer); at absolute minimum before run-triggers expose stored-URL crawls. **(A8) named gates**: migration = post-freeze governed path (this ruling + Code Review + QA isolation extension); queue/lease/processor/sweeper = Code Review + QA adversarial concurrency/recovery suite (double-processor no-double-lease, crash-mid-run orphan, kick-failure→sweep, retry-cap exhaustion — the 1.2 precedent); run-trigger UI = Design Review + Code Review. Owners: lead-backend-data-architect (migration+queue+processor+sweeper), aeo-seo-logic-engineer (live-fetch/socket-pinning), frontend-engineer (UI). **(A9) rule 9**: the whole loop must run under `npm run dev` with no Vercel cron (kick covers pickup + documented dev sweep trigger); operator reviews locally before any hosted deploy.
**Sequencing (confirmed):** the `runs` migration joins the governed post-freeze batch — now SIX items (properties persistence, content_items.title, needs_revision, competitors home, reviews home, runs); **properties lands with-or-before runs**. Run-trigger UI may build against the ruled queue contract in parallel (local fixtures only; ships only against the real table). This ruling does NOT clear pick-one-scan-source (M2 vs M6) or the M2/M14 kind-filters — wiring scan output into generatePlan stays gated as recorded.

**2026-07-10 · SCOPE AUTHORIZATION — governed post-freeze schema batch · Orchestrator AUTHORIZED (scope-level; nothing "done" until Code Review signs the drafted migrations; deviations return to Orchestrator first)**
Verified basis: frozen `properties` shape (0003 L80–112), frozen `content_items` CHECKs (0005 L77–102), doc 03 §3/§4, the ratified access split. **The batch ships as FIVE items in THREE migrations** (one file per table touched; independent down-migrations = independent rollback; staging first per ops/environments.md):
· **Item 1 — properties persistence: WRITE-PATH ONLY, no schema change.** Frozen shape already sufficient (url, platform CHECK + website-has-platform, connection_method CHECK default 'none', auth_ref vault-ref, composite-FK anchors for runs/audits). Verify-state column NOT authorized (derivable today; a real status column belongs to the Connections/vendor-wiring block via this same path). Scope: extend `createClientFromOnboarding` to insert the website property row + workspace properties create/edit seam (url/platform/connection_method) — **no delete surface v1** (on-delete-restrict FKs are the backstop). Conditions: onboarding idempotency/UPDATE-THROUGH replay must cover the property row (a divergent replay must not strand a stale property URL); connection_method never written connected-looking without a real connection; auth_ref never written by onboarding; URL inputs clamped like existing onboarding clamps.
· **Item 2 — content_items.title: AUTHORIZED** (migration 0009). `title text null` CHECK ≤200. NO backfill — existing rows null, UI renders honest "Untitled", never fabricates from body. Conditions: title is shipped client-facing text — included in M8 compliance pre-screen input when generated, M19 jargon rule at render; title participates in verdict binding (hash covers body + coalesced title — a post-verdict title edit invalidates verdicts).
· **Item 3 — needs_revision + R3 lifecycle: AUTHORIZED, exact scope** (migration 0009, same file — same CHECKs altered): (1) status CHECK gains `'needs_revision'` — one state only; (2) verdict binding = `body_hash text not null` maintained by BEFORE INSERT/UPDATE trigger (pgcrypto digest over body+coalesced title; backfilled) — structural, not app-trusted; verdict jsonbs record the hash reviewed; (3) strengthened approval CHECK replaces presence-only: approved/published requires quality_review passed IS TRUE AND compliance_review passed IS TRUE AND both verdicts' recorded hash = row hash; humanization passes IS TRUE required for machine-produced prose (blog/faq/caption/pillar), exempt for schema_copy + automation_level='human_only'; corner cases escalate back, never silently widened/narrowed; (4) `approved_by uuid` + `approved_at` with CHECK approved/published requires approved_by NOT NULL (rule 5 — a named human in the row; site_changes precedent); (5) transition table pinned in tests + enforced at actions/CHECKs: draft→in_review (producer); in_review→needs_revision (send-back, verdict recorded); needs_revision→in_review (new hash makes old verdicts structurally stale); in_review→approved (approval action, all CHECK conditions); approved→published ONLY via the publish path behind change management — **no publish wiring authorized in this batch**; demote-before-edit lives in the actions, QA-pinned; (6) NOT authorized: type-enum extension (review_response/pr_outreach — lands with the regulated-vertical/vendor activation work as recorded).
· **Item 4 — competitors: AUTHORIZED as NEW TABLE** (migration 0010); jsonb-on-clients REJECTED (write-floor mismatch — competitor mgmt is operator workflow, clients writes are agency_admin-only; both consumers need queryable rows). Shape: id, tenant_id, client_id, name ≤120 NOT NULL, domain null (bare-hostname grammar at the write seam), created_at; unique (tenant_id, client_id, lower(name)); RLS enabled+forced, composite FK → clients, client_viewer select own-client (M19 SOV renders), is_writer write floor. Per-client cap 10 app-enforced + QA test (⚑ ratify list).
· **Item 5 — reviews home: DEFERRED — explicit ruling, batch ships five.** No vendor wired, Reviews UI is P2, a raw-reviews schema before vendor semantics are known = the invention rule 7 prohibits. Aggregate stays in metrics(source=reviews); raw reviews port-side. Binding: the M15 Compliance precondition stands — no reply-send wiring passes review until a persisted status-bearing gate record exists; the reviews-home decision is a named prerequisite of the review-platform vendor-wiring block; R3 lifecycle is the intended gate-record shape once reply drafts get a legitimate content type.
· **Item 6 — runs: scope per the A4 conditions above** (migration 0011).
**Ownership + gates:** lead-backend-data-architect drafts the full batch (single owner); UI consumes contracts only after Code Review passes the migrations. Gates: Code Review (security + tenant isolation per doc 03 §4, incl. the strengthened CHECKs) + QA isolation-suite extension (competitors/runs trip the all-roles future-table tripwire by design; content_items blessing tests updated for the strengthened approval CHECK — the F1 automation-CHECK precedent) + documentation agent updates `docs/contracts/data-model.md` before the batch is declared done.

**2026-07-09 · Phase 1.10 COMPLETE — M19 White-label client dashboard · Code Review → PASS · Design Review → REJECT (rendered internal jargon) → fixed → PASS. ⇒ ENTIRE PHASE-1 MODULE BUILD (M1–M19) COMPLETE & GATED.**
Commits 25d3b14 (M19) + b6b969b (jargon fix). Client-scoped /clients/[clientId]/dashboard consuming every gated module read API (M3 visibility+signature moment, M3/M4 share-of-voice, M14 local, M16 ROI, M17 alerts, work-done log from site_changes+content, content calendar) read-only. **Code Review PASS: client-scoping verified end-to-end across 3 layers (RLS tenant_id+client_scope on every table + app-layer UUID/role guards + own-tenant-only theme read) — a URL clientId CANNOT leak another client's/tenant's data; env-less dynamic build passes, no 500.** Design Review PASS: white-label re-skin is REAL (token-driven via the frozen F2 engine + buildBrandKit contrast gate — proven with a full Coastal Realty re-theme, zero raw hex), honesty rule holds (ready|pending|failed per panel, dignified "measuring soon" pending states, no fake numbers), signature VisibilityScoreResolve moment reused correctly (raised-panel contrast, zero layout shift, reduced-motion). **Design Review Blocker (fixed): the content-calendar footnote rendered internal code "(M11)" on the client-facing white-label surface — the exact jargon-sweep exposure the earlier tickets warned about — corrected to plain client copy (the M## refs elsewhere are comments, not rendered).** Non-gating carried: anonymous calendar rows (no client-safe title field yet); single-palette tenants.theme (dual-palette reseller extension flagged); client_viewer guard's always-true conjunct. 2546 tests green.

**2026-07-09 · Phases 1.7 + 1.9 COMPLETE — M11 Social design · M12 PR entity-leverage · M18 Resource center — all gates PASS**
Commits be165e6 (M11) · b553765 (M12) · 7823933 (M18) + the caption doc-comment correction. All behind fail-closed deferred vendor ports (Higgsfield/Motion media, SocialPostingProvider, Anthropic answer + web search). Gate verdicts:
· **M11 Social — Code Review PASS + Content Quality PASS + Compliance PASS** (1 CR minor). Captions in locked brand voice governed like M8 (grounding + voice screen + social_caption pre-screen), media BRAND-FORCED server-side from the kit (no caller brand seam), structurally CANNOT auto-post (.schedule() never called; composed post pinned pre-approval; caption inherits M8's dual auto-publish-impossible guarantee). Schema gaps (media ref, composed post/schedule) flagged not forced.
· **M12 PR — Code Review PASS + Content Quality PASS + Compliance PASS** (3 CR minors). Entity Person/Org schema via M10's visible-text gate (no fabricated entity), sameAs-genuineness caveat + uncorroborated flag, human-assisted outreach with NO send path (no_grounding refuses before the LLM is called), egress-guarded crawl, plan-merge via the pre-wired M12 channel archetype.
· **M18 Resource center — Code Review PASS + Content Quality PASS** (clean CR). Playbook-scoped Q&A behind deferred Anthropic+web-search ports; the grader treats the provider as untrusted (citations verified against retrieval, fabricated sources stripped/flagged, confidence only lowered); feed shapes for M3/M8 shaped-not-wired; requireAuth per doc 05.
**Carried preconditions (real-output/vendor-wiring, join the M8/M10/M15 set — nothing publishes/posts/sends until these clear):** (a) **PLATFORM-GATE structured-signal gap** — cannabis.ad-platform-gate + health-life-insurance.special-ad-category are appliesTo:["ad"] only, so a cannabis/insurance CAPTION to Meta is NOT platform-gated by the social_caption pre-screen (fenced today: cannabis dormant + no transmit). HARD PRECONDITION: extend both rules' appliesTo to include 'social_caption' (reviewed seed-ruleset change) before any SocialPostingProvider connector is wired OR a regulated vertical activates for social. (Doc-comment in caption.ts corrected now to stop overstating coverage.) (b) **Credential-fabrication blindness** (M15 parity) — findUngroundedClaims covers only stats/superlatives; fabricated qualitative credentials/results in outreach ("licensed attorney", "certified by X") + fabricated remediation promises are a human + gate responsibility on real output — the #1 outreach/caption real-output re-gate item. (c) M18 Q&A semantic accuracy (grader verifies citation EXISTENCE not semantic support) + route regulated-claim answers to compliance-review at wiring. (d) pr_outreach + caption-generation content types (drafted as blog/faq analogs today) before regulated-vertical activation; the SEND/POST connectors (Phase 2/integrations) must route through both hard gates + human approval + change-management, pinned pre-approval as the only entry state. (e) M12 kind-filter precondition (M14 class) + M2/M12 entity double-list. CR minors: M11 decorative clientId param; M12 blog-literal compliance type / org-schema-no-fix intent / unbounded history fetch.

**2026-07-09 · Phases 1.6 + 1.8 COMPLETE — M14 Local SEO · M15 Review management · M16 ROI/attribution · M17 Alerting engine — all gates PASS**
Commits cbb5a9a (M14) · 578aa45 (M15) · 275b771 (M16) · 4d18a1a (M17). All behind fail-closed deferred vendor ports (GBP, review-platform, GA4/GSC/call-tracking, + signal sources). Six gate verdicts, all PASS:
· **M14 Local SEO — Code Review PASS** (2 minors). Multi-location NAP/completeness/local-pack, local schema via M10's visible-text gate (no fabricated NAP ships), SSRF egress guard reused, playbook-intensity, absent≠zero honesty. Minors: deep import of a non-public aeo-audit util path (skill owner should promote those normalizers to the public index); unbounded readLocalHistory fetch. **HARD precondition: M2 readAuditHistory must exclude kind:local_assessment rows before live wiring (or local scores pollute the AEO trend line); M2↔M14 local double-list for the plan-integration owner.**
· **M15 Review management — Code Review PASS + Content Quality PASS + Compliance PASS** (all 3). Cannot-auto-send verified 4 structural ways (read-only monitor port, pinned draft status, no send path, action persists nothing); sentiment/velocity honest (unconnected excluded not zeroed); real-estate replies screened for Fair-Housing under the correct review_response type; content_items schema-gaps flagged not forced (aggregate signal homes in metrics(source=reviews)). **Content Quality condition — real-output re-gate checklist: tone-appropriate-to-a-review, substance/non-boilerplate, negative-review handling, and CRITICALLY fabricated remediation PROMISES ("we've fixed/refunded X") which findUngroundedClaims is structurally blind to (no stat/superlative). Compliance preconditions: the 1.7 reply-SEND connector must require BOTH gates + human approval + change-management/rollback and never be import-reachable from respond.ts; structured-signal gap (endorsement-disclosure containsTestimonial/hasAffiliateRelationship, state-licensing jurisdiction) before regulated verticals do review replies; a PERSISTED status-bearing gate record must exist before any reply is sent; human checklist for public-communication risks (PII/relationship confirmation, defamation/retaliation, liability admission — none machine-checked).**
· **M16 ROI/attribution — Code Review PASS** (2 minors). Credential-hygiene (masked, host-pinned, SSRF-closed, leak-swept) + causal-honesty (correlation-not-causation by type, absent≠zero, no extrapolation, no ROI overclaim) both held under adversarial reading. Minor: the live action's summary transiently lists un-homed metrics (moot until a vendor-agnostic adapter lands; narrow then). metrics.source gap (form_fills/crm/attribution unhomed) refused at the seam + flagged.
· **M17 Alerting engine — Code Review PASS** (1 minor). No-double-write confirmed structurally (crawler_blocked stays M5-owned, auto_rollback_fired only via the change-mgmt sink; SignalBundle can't carry them; persistWriterAlerts refuses non-writer types); 7 classes map to frozen CHECK types (no invention); M5 dedup reused fail-closed; missing signal → unknown never fabricated. Minor: a "not exported" doc comment on an exported function — correct to "exported for ./sink only". ⚑ thresholds flagged for ratify. Wiring seams (sink injection, competitor/schema/review/uptime signal sources) flagged.

**2026-07-09 · Phase 1.5 COMPLETE — brand-production + authenticity pipeline (M7 Brand Kit · M8 Content Production · M9 Humanization/Detection · M10 Schema) — all gates PASS**
The FIRST content-generating block; governed by the two HARD gates (Content Quality + Compliance) on top of Code Review. All four modules built + gated (M7 and M10 have their own records above; M8/M9 below). Real vendors (Anthropic LLM, humanizer, AI-detectors) are DEFERRED behind fail-closed ports — the gates reviewed pipeline PROPERTIES + guardrails; actual generated OUTPUT is a wiring-time re-gate.
· **M8 Content Production (commit ad297f5) — Code Review PASS** (3 minors). Playbook-mapped, brand-voiced drafts behind the ContentGenerationProvider port; auto-publish STRUCTURALLY IMPOSSIBLE (status=draft + automation_level pinned + the frozen content_items_reviewed_before_approval CHECK unreachable while review columns NULL — code pin AND schema pin, verified against 0005); no-brand-kit refusal; compliance-as-generation-constraint (never self-clears).
· **M9 Humanization + AI-detection (commit 966aa73) — Code Review PASS** (2 minors). Multi-detector panel (pilot 2-3), verbatim scores, flag-not-force-pass, meaning/voice drift; advances only to in_review, never writes review columns (belt-and-braces with the CHECK).
· **Authenticity-pipeline HARD GATES — Content Quality → REVISE → remediated → PASS · Compliance → PASS (scoped) → item-3 fixed early.** Content Quality REVISE (R1 anti-fabrication digit-substring under-flagged fabricated stats; R2 AEO-formatting check unwired) + Compliance MANDATORY item-3 (M9 rewrites the body AFTER M8's compliance pre-screen, so a humanizer could drop a required disclaimer or introduce a Fair-Housing violation uncaught by the marker-only drift check). **All four bounded fixes landed + re-gated PASS x3:** (R1) value-exact numeric-token grounding — fabricated stats now flag in both M8 and M9 (98%≠1980, 20%≠2024), probed empirically no false-ground; (R2) evaluateFaqAnswer + blog/pillar opening-directness wired as flags; (item-3) M9 re-runs checkCompliance on the humanized body, flags a new-block-or-dropped-required-element, keeps the compliant original, attaches a fresh prescreen on the shipping body — never-self-approve preserved (verified byte-unchanged write path); (FIX4) M8 screens its own output for voice.dont phrases. 2170 green.
**1.5 wiring-time PRECONDITIONS (carried — real-output/vendor-wiring gates, join the M10 + write-methods + crawler precondition set; nothing regulated publishes until these clear):** (a) **approval-transition lifecycle (R3)** — the not-yet-built approval/verdict-write actions must require quality_review.passed===true AND compliance_review.passed===true AND humanization.passes===true (not mere presence), add a send-back/needs_revision status, and invalidate prior verdicts on any body mutation (re-humanize) or bind verdicts to a body version/hash (owner: review-gate/approval block); (b) **dormant-vertical structured-signal threading** — thread jurisdiction/licensedStates/hasAgeGate/lastVerified/platform into ComplianceContent in BOTH M8 ground.ts and M9 authenticate.ts before cannabis/health-insurance content activates; (c) **real generated+humanized OUTPUT** passes Content Quality + Compliance on real prose once the Anthropic/humanizer/detector adapters are live (real estate first); (d) the real-output re-gate list (voice-prompt construction enforces descriptors/do/dont/samples; tone/register/substance/non-generic; humanization-degraded-readability via original-vs-humanized diff — needs pre-humanization body retention, AUTHENTICITY_ORIGINAL_BODY_GAP); (e) resolveThresholds semantic-clamp guard at wiring. ⚑ Ratify: M9 thresholds (pass-line 0.30, min-detectors 2, unanimous); M8/M9 numeric grounding is value-level (sign/unit-insensitive).

**2026-07-09 · Phase 1.5 (part) — M10 Schema generation · Code Review → REJECT (review-fabrication Blocker) → remediated → PASS · Compliance Review → PASS (scoped to real estate)**
Wraps the frozen schema-generation skill + adds the "must match visible text" gate (fabricated FAQ answers / prices / business names rejected with the unmatched claim named); produces on-demand JSON-LD, feeds the change-management path as a ready-only DesiredChange (changeType 'schema', before=on-page JSON-LD baseline), NEVER wires publish. Code Review Blocker (fabricated aggregate review counts/ratings) closed via the authorized skill tightening (freeze-log above). **Compliance Review PASS is SCOPED: M10 ships for real estate (GG client zero, the only live vertical) + the remediated review/aggregate schema, under a LOAD-BEARING CONDITION — every schema DesiredChange carrying free-text/jurisdictional fields (description, FAQ answer, headline, name, areaServed) must route to compliance-review before the human approves the preview (doc 07 §1.5 hard gate; the visible-text gate is an accuracy control, orthogonal to legality).**
**HARD PRECONDITIONS (prospective — all behind the Gate-1a dormancy wall; must clear before the vertical/field goes live):** (i) **schema has no compliance pre-screen** — the M8 prose path calls checkCompliance, M10's schema path does not, and the compliance engine has no `'schema'` ComplianceContentType. Before cannabis/health-insurance schema go live: add a `'schema'` content type + a flattener lifting JSON-LD text/areaServed/dietary fields into ComplianceContent and run checkCompliance inside produceSchema (mirrors ground.ts) — this also closes the live-vertical residual that RealEstateAgent/Person `description` is warning-gated + divergable, uncaught for Fair-Housing steering (42 U.S.C. §3604(c)); (ii) **restaurant `suitableForDiet` dietary/allergen tokens** are pass-through — must be substantiation/attestation-gated before restaurant go-live (FDA 21 CFR 101.91 for gluten-free; certification for Halal/Kosher); (iii) **review genuineness** — on-page-match doesn't prove reviews are real; aggregate rating/count data must be sourced only from M15 genuine review feeds, never synthesized; (iv) glyph/word-aware individual-review rating matcher (follow-up so per-review ratings can be gated without false-rejecting ★-markup). Non-review + real-estate schema sound as-is. This precondition set joins the write-methods wiring + crawler DNS-rebinding preconditions — nothing publishes to a live regulated-vertical property until (i)/(ii) land.

**2026-07-09 · Phase 1.5 (part) — M7 Brand Kit engine · Code Review → REJECT (B1 Blocker) → remediated → PASS**
Commit 144bdff + B1 remediation commit. Wraps the frozen brand-kit-design-token skill to ingest a client brand into a LOCKED, versioned (insert-only immutable) kit; claim-sourced tenant, is_writer floor, contrast-unresolvable brands refused at lock, partial-input honesty (defaulted vs missing). **Code Review Blocker — the `typography.scale` B1 CSS-injection gap** (first caller-controlled free-text token path to reach emitted CSS unvalidated; stored-injection into an immutable kit). Closed via an AUTHORIZED post-freeze skill change (see the freeze-log entry above — gate at the emitting layer for all callers + M7 defense-in-depth; 0 grammar bypasses on re-review; valid scales byte-unchanged; 2029 green). Operator-vs-admin write-guard decision: Code Review recommends ACCEPT AS-IS (requireOperator honestly mirrors the is_writer RLS floor; tightening locking to admin-only would also require changing the frozen brand_kits_insert RLS — a separate post-freeze product call, flagged in code). Carried minors: DB doesn't enforce brand_kits row immutability at the schema level (frozen-schema scope); concurrent-revise collapses to generic write_failed copy; provenance rides assets.ingestion jsonb (proposed brand_kits.provenance column, post-freeze).

**2026-07-09 · Phase 1.4 COMPLETE — M4 Competitor citations + M5 Crawler/render monitoring + M6 Content decay · Code Review → PASS (0 blockers/majors, 3 minors)**
Range f496023..62e0d94 (commits 176f7ce M4, 86680ef M6, 62e0d94 M5), 44 files additions-only, frozen layers untouched (import-only), 1919/1919 green. **The intelligence suite (M2–M6) is now fully built + gated.** All three reuse the gated crawl layer + egress guard — SSRF closure verified to hold per module (M4's competitor URLs, the most attacker-influenceable input, refused pre-fetch via sync literal check + DNS guard); no fetch bypasses crawlSite. M5 writes `alerts` (type=crawler_blocked, claim-sourced tenant, is_writer floor, dedup fails-closed); M4/M6 persist nothing (structurally — no Supabase client on the persist path). Honesty spine verified in code (M4 n-of-m excludes uncrawlable; M5 unknown≠blocked; M6 age-unknown≠stale). No client-facing text → Content Quality not triggered. **Minors (all latent/documented, owner = build agents):** (1) M6-decay ↔ M2-audit-freshness could double-list a page's refresh IF a future integration concatenates both into one generatePlan — NOT reachable today (no scan source is wired into plan generation; the decay/audit projections have no caller); the plan-integration owner must pick one source or dedup by URL before wiring; (2) M5 concurrent-run dedup race (benign, self-heals on acknowledge; the dangerous failed-read-spam case is already closed fail-closed; a partial unique index would make it atomic); (3) doc-silent thresholds confirmed greppable → ⚑ ratify list (M4 score≥70/margin≥15 + shallow-crawl bounds; M6 DECAY_THRESHOLD_FLAGS; M5 AI_CRAWLERS 15-token enumeration).

**2026-07-09 · Phase 1.4 (part) — M2 Audit Engine + M3 Visibility Tracker · Code Review → REJECT (SSRF) → remediated → PASS**
Commit 9e83459 + SSRF remediation commit. M2 wraps the frozen aeo-audit skill (bounded same-origin crawler → skill scoring → fixes flow into the real generatePlan/audit-merge → tenant-scoped `audits` persistence); M3 samples AI-engine citations via the CitationDataProvider port → documented score formula (⚑ operator-ratify) → append-only `visibility_results`. No client-facing text → Content Quality not triggered (confirmed). Tenant isolation, honesty (no invented numbers; absent≠0; per-page/coverage structural), telemetry redaction all verified clean first pass. **Code Review Major — SSRF: the crawler fetched attacker-influenceable property URLs with only a protocol check, so internal/link-local/RFC1918/cloud-metadata targets were reachable server-side (redirect- and link-pivot escapes were already closed; the direct-internal-target path was open + unflagged). ORCHESTRATOR DISPOSITION: CLOSE THE CLASS.** New `crawl/egress-guard.ts` blocks the full private/loopback/link-local/ULA/CGNAT/metadata range set (IPv4 + IPv6 incl. mapped/compat unwrap), classifies the WHATWG-canonicalized host undici connects to (so decimal/octal/hex re-encodings can't create a guard-vs-connect mismatch), literal-sync then DNS via a REQUIRED injected resolver (any-blocked-wins, fail-closed), wired at every fetch entry; blocked → uniform `blocked_address`, no status oracle. Re-review attacked 23 literal encodings + 10 DNS cases → 0 leaks; PASS. Minors fixed: NUL byte in derive.ts; per-request 15s timeout + 120s crawl budget. 1793/1793 green.
**Tickets:** (M2/M3-i) **DNS-rebinding TOCTOU** is the documented residual — undici socket-pinning to the vetted IP MUST land before broad multi-tenant/white-label crawler exposure (hard precondition, like the write-methods wiring); (ii) trailing-dot FQDN `localhost.` hardening nit (sync check; DNS path already fail-closes it); (iii) pre-existing NUL byte in `write-methods/cloudflare/qa-rollback.test.ts` (1.3 territory, functional, separate cleanup pass). **⚑ Launch-threshold flags for pre-Gate-1a ratification:** crawl bounds 30 pages / depth 3 / 1.5 MB; request timeout 15s; wall budget 120s; RENDER_VISIBLE_MIN_WORDS=30; M3 visibility score formula; M3 frozen-schema limits (no run-metadata/coverage history, no per-market column).

**2026-07-09 · Phase 1.3 method 4/4 — Cloudflare edge worker · Code Review → REJECT → remediated → PASS · QA rollback gate → PASS (re-confirmed post-remediation) — 1.3 BLOCK COMPLETE**
Commits 7feb288 (+ remediation & QA-suite commit). The API-less method: a per-client-domain worker rewrites HTML in flight (HTMLRewriter) from a versioned KV rules manifest; the adapter writes rule slots, never origin content. Inversions documented + pinned: before-capture = rule state (null first-class); rollback STRONGER than methods 1-3 (removal reveals the untouched origin); verification manifest-level only — render effect belongs to MONITOR (~60s KV propagation + primed-cache/304 caveats). Worker fail-open proven by response-object identity on every failure path (one carve-out honestly documented: origin-fetch rejection has no origin response to serve); injection escaping breakout-proof under hostile probing; rewritten responses no-store + validators + surrogate-control-class headers stripped.
**CR Major 1 — the stale-base manifest race** (two same-property operations read the same base; both verify byte-exact; the first write is silently lost — a lost REVERT would leave a rule live while its row reads reverted): KV has no CAS, but ALL manifest writers are in-house, so — **ORCHESTRATOR + CODE REVIEW DISPOSITION: KNOWN RACE with a HARD PRECONDITION — this method must not be wired to live client properties until per-property write serialization exists at the wiring seam, arriving with a serialization proof test (QA's binding expectation).** Prose corrected everywhere; the race pinned by a deterministic two-writer interleaving test that re-opens the disposition if adapter-level detection ever improves. **CR Major 2 — bare `title` selector would have clobbered inline-SVG accessibility titles page-wide:** fixed via head-scoped selectors applied at the sole production entry (compile-time-exhaustive mapping, reviewer-accepted seam) + an independent headClosed handler gate proven under bare selectors. QA re-confirmed PASS post-remediation (97/97 suite untouched; 1638/1638 full).
**First-live-deploy canaries (consolidated, in-code + here):** real scoped selectors against real lol-html; CF error-code semantics (10009/403); raw KV byte echo; KV values-GET read-after-write consistency. Non-gating backlog: duplicate JSON-LD on pathological multi-`</head>` markup; map-or-throw hardening in scopeRewriterSelectors.

**2026-07-09 · Phase 1.3 method 3/4 — Wix write method · Code Review → REJECT → remediated → PASS (+ disposition sign-off) · QA rollback gate → FAIL → dispositioned + re-scoped → PASS**
Commits 350ef90 (+ remediation & QA-suite commit). Live-immediate honesty (no staged layer — before-capture + verified revert IS the safety story; applied_at correlation sound here, with the resumed-rows caveat). Grammar structurally kills the system-field class (no leading `_`, lookalike probes pinned) and app-namespaced collections; account-level keys make the wix-site-id pin the isolation boundary (on every request, GUID-validated, host exact-pinned). All three Webflow carries (α echo-class kill via shared refuseBaseUrl, β mirror-corner test, γ 429 pins) confirmed CLOSED — the credential-echo class is dead.
**Both gates independently found the same core defect (WIX-QA-2 = CR Major 1): a client-staff CMS edit landing inside the data write's GET→PUT window is silently overwritten (Wix Data v2 is last-writer-wins full replace — no conditional/partial update; the race cannot be closed at the transport). ORCHESTRATOR + CODE REVIEW DISPOSITION — ACCEPTED RESIDUAL:** overclaims corrected everywhere, prominent adapter-header residual section (window/why/size/operator-facing consequence: correlate item `_updatedDate` vs `applied_at`/`reverted_at` when a client reports a vanished edit), pinned in TWO independent suites (engineer's adapter pin + QA's pipeline pins, disposition-dated) so any behavioral change re-opens the disposition. CR Major 2 genuinely FIXED: page-SEO writes now verify the NON-target seoData attr from the echo against the pre-write capture (replace-semantics reality or unset-materialization → loud write_verification_failed; survived reviewer attacks incl. echo-omits-seoData). QA red-sensitivity: 7 injected defects across the two passes, all turned suites red (incl. both residual pins proven load-bearing, not vacuous).
**Carried:** (δ) pin the residual-test's fake hook via _updatedDate/journal so a broken hook can't make the pin vacuous (fold into 4/4 commit); (ε) first-live-write canary (pages merge semantics, data-item system-field requirements) — joins ticket (ii) per-property wiring; ticket (iii) stuck-previewed ops surface RE-FLAGGED as more acute for live-immediate Wix (a verification failure leaves the LIVE site divergent while the row reads previewed); operator-facing framing for the residual when Wix wiring UI ships.

**2026-07-09 · Phase 1.3 method 2/4 — Webflow write method · Code Review → PASS (0 blockers/majors, 4 minors) · QA rollback gate → PASS (no defects)**
Commit 042e1d8 (+ QA suite commit). Staged-only by design: no locator can express a publish; rollback restores the staged capture even under unpublished drift; live snapshot proven untouched via positive route whitelist + journal sweeps. Site pinned at the API level (page siteId asserted read/pre-write/echo; collections proven against the pinned site's list; api.webflow.com exact-pin closes SSRF). Mirrored-OG refusal double-checked pre-write AND post-write; slug fields refused as server-normalized-irreversible; 429 → new shared `rate_limited` code, no automatic retries. QA: 122 adversarial tests through the real ChangeManager (round-trip exhaustive w/ grammar-completeness pin, staged/published boundary under drift, QA-1 crash windows re-proven through this adapter, auto-rollback modes, partial batch w/ 429 + re-run continuation), red-sensitivity proven by 5 injected defects (each turned the suite red: 99/2/2/84/5 failures). Also fixed in 042e1d8: WP carried item (i) (unparseable-base-URL echo).
**Carried to Wix (3/4) commit — MANDATORY per reviewer (third carry = rejection):** (α) WP constructor's query/fragment branch still echoes the raw base URL (`?token=...` class) — hoist a shared refuse-without-echo helper so the class dies; (β) test the post-write mirror-flag branch (add FakeWebflow `setMirror()` + one-shot flip hook, retiring QA's harness reach-ins); (γ) ~10-line 429 pins for mid-batch + revert-direction row-status truth. **New product ticket (named owner needed before Webflow serves real clients): Webflow "applied" = staged-only — MONITOR/auto-rollback correlation keyed to applied_at is unsound for this method (live effect lands at the client's next publish), and operator UI needs "staged; goes live on next publish" framing + a publish-aware correlation signal.** Joins carried items (ii) per-property adapter wiring and (iii) stuck-previewed ops surface from the WP record.

**2026-07-09 · Plan-regeneration + duplicate-guard slice · Code Review → REJECT → remediated → PASS · Design Review → PASS w/ findings → re-check PASS (all closed)**
Commits be9b866 + 2267fed (build) + 10d0e82 + f8aafa3 + 5bc0bcb (remediation). Delivered: `regeneratePlanForClient` (idempotent, typed reasons), onboarding idempotency key (one UUID per run, becomes the row id — lost-response retries recover instead of duplicating), runtime input clamp, redacted plan-failure telemetry, dashboard Generate-plan control (agency_admin-only, live verticals, incl. zero-task residue), HEAD count:exact aggregates (no silent undercount at the PostgREST cap). **Code Review's Major (caught pre-merge): a replay after the operator EDITED the payload silently returned the original row while the UI claimed the edits saved. Fixed by Orchestrator-directed UPDATE-THROUGH reconciliation** — divergent replays update the row (RLS-scoped, sanitized values only) and a vertical change supersedes the replay-run's own plan before regenerating; update failure returns a distinct honest error, never ok-with-stale. Re-review attacked jsonEqual false-negatives, cross-tenant/clamp bypass, supersede over-reach, partial-failure coherence — PASS. Design re-check confirmed the full recovery loop (warning copy → dashboard → control), approved the 70/30 negative/ink error-text mix (all combos ≥4.83:1; sentinel pins why plain --negative fails Signal-light), count-up retarget, full-width errors, StatusAnnouncer a11y. 1005/1005 tests.
**Tickets from this slice:** (A) **GRADUATION CONDITION — must land before a second vertical goes live:** vertical-aware `ensurePlan` supersede (a triple-failure corner can strand a wrong-playbook tasked plan that the Generate control won't offer to fix; live→live vertical edits would widen it to a Major); (B) governance comment at the tasked-plan sweep should state its freshness-window assumption; (C) replace ACTIVE_VERTICALS array mutation in tests with vi.mock; (D) /clients page plan row + control before client books exceed the dashboard's 6 cards; (E) step-4 error heading variant for the unreachable case (polish); (F) systemic `--negative-text` 4.5:1-gated derived token via the post-freeze path (tactical color-mix shipped meanwhile).

**2026-07-09 · Phase 1.3 method 1/4 — WordPress write method · Code Review → REJECT → remediated → PASS · QA rollback gate → FAIL → fixed → PASS**
Commits 92a939a (adapter) + the remediation commit. First code that can write to a client site. Code Review round 1: 1 Major (http: base URLs accepted → Application Password would egress cleartext; fixed: HTTPS-only at construction, no opt-in, bypass-probed) + 5 minors (meta:[] PHP quirk → accurate target_missing; transport error detail whitelisted, never err.message; userinfo targets refused without echo; fake-WP registeredMeta fidelity mode + 400 envelope; FetchPort pinned redirect:"error"). **QA gate found the block's most important defect — QA-1: the 1.2 change-management manager corrupted the rollback baseline on apply-retry after a DB crash post-site-write (re-baselined diff.before to the change's own after → rollback became a verified no-op while the site kept the change). Invisible to the 1.2 suites (they fault-injected only the adapter, never the store); exposed by the first real adapter.**
**AUTHORIZED POST-REVIEW-PASS CHANGE to the reviewed 1.2 layer (freeze-log entry):** manager.ts apply() (+30/−9) + 1-line additive PipelineWarning union (`resumed_after_partial_apply`, types.ts). When the live re-read ≠ persisted previewed before AND = the change's own after, the persisted before stays the rollback baseline (safe in both indistinguishable cases: crash-resume and third-party-set-same-value) with a distinct warning; legitimate drift re-baselines unchanged. Orchestrator authorized; Code Review re-review attacked all four axes and signed off (fix proven load-bearing: the QA test necessarily fails against the pre-fix manager); QA re-verdict PASS with new pins: double-crash loop, warning distinctness, third-party twin, and a pinned honest residual (adapter-invalid previewed before → loud invalid_value on every retry, row never leaves previewed — backlog: ops surface for stuck-previewed rows). QA suite: 139 adversarial tests incl. exhaustive locator-kind round-trips with a grammar-completeness pin, auto-rollback policy modes (off/flag/execute), partial-batch prefix rollback.
**Carried from this block:** (i) unparseable-base-URL error still echoes the raw URL (one echo path left; fold into method 2/4's commit); (ii) production wiring must construct WordPress adapters PER PROPERTY (method-keyed MapAdapterRegistry + per-property pin fails closed via property_mismatch, but the wiring step must document/handle it); (iii) ops surface for stuck-previewed rows (QA residual). Next: Webflow (method 2/4).

**2026-07-09 · Real-data slice — plan/task persistence + live operator dashboard · Code Review → PASS · Design Review → REJECT → remediated → PASS**
Commits 2d01e32 (persistence) + cc30b4a (onboarding shows the persisted plan) + ea27d58 (live /dashboard, APP_HOME) + df85ca5/4da0330 (design remediation). **Code Review PASS first pass** (0 blockers/majors, 6 minors): independently re-verified the full claim chain down to frozen RLS/composite FKs; hostile-payload test proves smuggled roadmap/tenant fields are dead weight; automation clamp at the persistence seam sweeps all seed playbooks — 'auto' survives only if the generator emitted it. **Design Review REJECT (narrow, copy-honesty) → PASS after remediation**: visual system/tokens/motion/mobile/honesty-rule all passed first pass; 4 gate-blockers were copy/layout (stale "preview" framing on dormant verticals, phantom "retry from the dashboard" affordance, outdated "landing next" status, pipeline-card dead space) — all fixed + one remediation-introduced footnote overreach caught on re-check and amended. Reviewer drove every onboarding outcome live (saved+N tasks / dormant-vertical save / plan-write-failed / client-write-failed / slow-write hold) via a write-capable stub. 710/710 unit tests; no migrations touched.
**Carried tickets from this slice:** (a) duplicate-client footgun — lost-response retry has no idempotency key AND no plan-retry control exists; both land in the plan-regeneration slice (owner: lead-backend-data-architect + frontend); (b) dashboard aggregates silently truncate at the PostgREST ~1000-row cap — server-side aggregates before task volume grows; (c) runtime clamp for `locations` shape/length at the write seam; (d) redacted server-side telemetry for plan-write failures; (e) **vertical activation must backfill plans for already-saved dormant-vertical clients** (the approved copy promises it — owner: aeo-seo-logic-engineer, due with vertical activation); (f) chart tooltip hardcodes "Share of voice" (pre-existing; additive `seriesName` prop needs the post-freeze Orchestrator+Code Review path); (g) internal jargon sweep ("M3", "Phase 1.8") before any client/white-label exposure; (h) polish: prettify suggested client name, step-4 title truncation at 1280px, dead `h-full` on work-row cards.

**2026-07-08 · "Make it real" (Option A) — login + protected shell + client persistence · Code Review → REJECT → remediated → PASS**
Security check PASSED first pass: no cross-tenant/unauthorized client write constructible (tenant_id claim-sourced, requireRole("agency_admin"), RLS `clients_insert with check` backstop). REJECT was a robustness Major: env-unset 500'd every route + a false "never a 500" code comment. Remediated (non-throwing `tryGetPublicSupabaseConfig` guards middleware + verifiedClaims → fail closed graceful; signIn errors genericized server-side). Focused re-review PASS: boot-with-no-env proves 0 route 500s; env-SET security path byte-identical (cannot fail open); 675 unit + 399 isolation green. **Option A slice COMPLETE and reviewed — pending operator live-provisioning (apply migrations 0007–0008, enable hook, seed tenant #1 + login, set env) to sign into the live app.** Full loop built: login → protected shell → onboarding persists a tenant-scoped client → /clients reads it back. Deferred to next slice: plans/tasks persistence + operator dashboard reading them.


**2026-07-08 · Auth-claims + session layer · Code Review (security + tenant isolation, Extra-depth) → PASS — RATIFIED (0 blockers, 0 majors, 1 non-exploitable minor)**
Reviewer independently re-derived properties on a fresh scratch DB (applied 0001–0008, seeded two tenants, called the hook with forged events) rather than trusting the committed tests. All 7 adversarial checks: forgery dropped + re-derived; fail-closed confirmed; **Option 2 reserved-claim resolution ratified sound + complete** (proved a smuggled `role:agency_admin` in the reserved claim grants zero app authority — 0 rows); hook EXECUTE locked to supabase_auth_admin, search_path locked, adds no non-`authenticated` policy (posture invariant holds); session/guards read only signature-verified claims; viewer client_id integrity proven end-to-end; no secret/PII leakage. Minor: hook passes the incoming reserved `role` claim through (by design, proven non-exploitable — RLS reads only `user_role`, PostgREST SET ROLE needs membership); optional defense-in-depth, deferred. **This authorized frozen-foundation change is fully ratified (Orchestrator + Code Review).** 667 unit + 399 isolation green.

**2026-07-08 · Auth-claims + session layer (Option A "make it real", slice 1) · BUILT — awaiting Extra-depth Code Review security gate**
Authorized additive foundation change (migrations 0007 hook + 0008 helper): the Custom Access Token hook mints `user_role`/`tenant_id`/`client_id` SERVER-SIDE from `tenant_users` keyed off the authenticated `auth.uid()`; fails closed on no membership; discards/never-trusts incoming app claims; `client_viewer` structurally cannot influence its own claims. Session/guard helpers (`src/lib/auth/`) read verified claims via `auth.getClaims()`; refresh-only middleware; email+password server actions (no login UI yet — next slice).
**ESCALATION RESOLVED (Orchestrator decision + Code Review to verify):** the frozen RLS read the app role from JWT `role`, which is PostgREST's RESERVED db-role claim → would break the first logged-in data query. Orchestrator chose **Option 2** (app role → non-reserved `user_role` claim; PostgREST's `role` stays `authenticated`, the request role every policy targets AND every isolation test exercises — test fidelity was the deciding factor, over introducing grantable app Postgres roles that would run prod in an untested role context). Implemented as migration 0008 (`app.user_role()` reads `user_role`) + coordinated hook/harness/db.ts/parse-claims/docs rename. **All 399 isolation tests pass on the new claim — the rename preserved every isolation property (re-proven, not assumed).** 667 unit green. Frozen-foundation change logged here per CLAUDE.md rule 1; Code Review security gate ratifies next.
Provisioning (operator, docs/ops/environments.md): enable the Custom Access Token hook in the Supabase dashboard + create tenant #1 + operator agency_admin row (copy-paste SQL provided).


**2026-07-08 · 1.2 change-management layer · CLOSED — Code Review (rollback safety) PASS + QA (rollback suite) PASS + all findings remediated**
QA gate PASS: every rollback-safety invariant has a regression test proven to fail-red if its guard breaks (confirmed by injecting each bug); +6 tests incl. a real coverage hole (store optimistic-transition guard through RLS — DB CHECKs enforce field-presence, not transition direction). Remediation (integrations-engineer): [MAJOR] credential leak via util.inspect/console.log CLOSED (ES #private field + inspect.custom; test proves the secret prints on no standard logging path); [Minor 2] applyBatch re-asserts single-client; [Minor 3] drift-clobber + write-before-DB ordering documented in-code; [Minor 4] stub guard aligned to 3 more DB CHECKs. 651 unit + 385 isolation green. QA verified manager.ts byte-identical to committed after its regression-injection technique. Detail below:

**2026-07-08 · 1.2 change-management layer · Code Review (rollback safety) → PASS (0 blockers, 1 major, 5 minors)** — adversarial: reviewer attempted a bypass of all 9 doc 04 §2 hard requirements, none succeeded; verified against real RLS + CHECKs (640 unit + 384 isolation incl. 21 live-PG pipeline). QA rollback-suite gate still running (both must pass to close 1.2). Dispositions: **Major** (vendor credential leaks via util.inspect/console.log — connectors/types.ts) NON-BLOCKING for 1.2 (no live creds flow yet) but MUST land before the first live adapter in 1.3/1.4 → integrations-engineer. **Minor 1** (`site_changes.diff` gains `target` key) — RATIFIED 2026-07-08 (Orchestrator + Code Review, schema-safe); contract updated (data-model.md §3/§7). **Minor 2** (applyBatch re-assert single-client), **Minor 3** (document drift-clobber + write-before-DB ordering), Minors 4–5 (stub CHECK parity, tripwire factory-shape note) → batched into the post-QA remediation pass. This DB payload-convention clarification does not alter the frozen schema (CHECK only requires a jsonb object).


**2026-07-08 · 1.1 Industry Playbook Engine + onboarding · Code Review → PASS (0 blockers, 0 majors, 7 minors) · Content Quality → PASS (gate cleared) · 1.1 CLOSED**
Scope: 5 seed playbooks (doc 02-faithful, test-pinned incl. Gate 1a `ACTIVE_VERTICALS=["real-estate"]`), real plan generator (proportional channel allocation, deterministic — 50-run byte-identity probed; doc 03 §6 automation clamp verified on M10/M13), onboarding flow (dormant-vertical gate enforces Gate 1a in code; truth-aligned copy), 557/557 tests. Frozen-file string touch (onboarding-plan-reveal default subheading) EXPLICITLY SIGNED OFF (verified one-line diff). Post-freeze additions confirmed contract-preserving: dualModeTenantCss (both-or-nothing fallback; 12 hostile payloads refused — B1 lineage holds, verified against React 19 raw <style> rendering), entrance choreography (SSR-identical by construction, dual reduced-motion gates), mode toggle. Two independent gates converged on the same stub-generator bug earlier in the cycle — governance working as designed.
**Carried open items (non-gating, owners assigned):** [aeo-seo-logic-engineer] broaden auto-clamp beyond M10/M13 to all non-detection modules; stale RED docblock in audit-merge.test.ts; asAuditResult element validation; duplicate impact wording on audit-merged tasks (MUST land before audit path is user-visible in 1.2); schema_profile in one evidence string; module codes in operator diagnostics (before client-visible audit UI); nap_directories provenance comments; vertical-tuned audience nouns when dormant verticals activate. [designer/frontend] newline in scope-id sanitization (fold into existing F2 defense-in-depth ticket); entrance.tsx header comment (box-shadow bloom); mode-toggle external-writer desync; optional "we'll flag you" → "we'll let you know".
**M1b Playbook Generator:** deliberately deferred within 1.1 — needs the M18 Claude+web-search stack (Anthropic API key not yet provisioned); built when M18 infrastructure lands, activated only after Gate 1a per doc 07.


**2026-07-08 · Operator brand v1 consolidated app-wide (dual-palette) + motion signature · post-freeze additions logged**
Operator decisions: pass-3 blue-depth look adopted; BOTH palettes kept as the app's light/dark modes; structure locked (roles: primary blue, highlight cyan, red = urgency, glow language, Geist, hero bubble), VALUES float (font/color changes remain cheap; additive-color path proven). Implemented via additive `dualModeTenantCss` formatter — frozen engine/token contract byte-untouched; Signal fallback preserved both modes. Pass-2 Core Orange/Alachua retained as available accents, not leading.
**Post-freeze motion addition (operator-directed):** entrance choreography as the brand motion signature, framed inside frozen moment #5 — rise 14px + fade 520ms cubic-bezier(0.22,1,0.36,1), 90ms sibling stagger, hero glow bloom 800ms @ +420ms, counters/gauge +400ms after tile entry; pure CSS, SSR-identical markup, transforms/opacity only, dual reduced-motion gates (verified: instant final states). Logged against the freeze record per governance; code-review coverage folds into the 1.1 gate.
Phase-1 flag (standing): dual-palette `tenants.theme` schema extension owned by lead-backend-data-architect at M7.


**2026-07-07 · 0.4 F2 design system · Code Review (code quality/security, per CLAUDE.md rule 3) → REJECT (1 blocker, 4 minors) → remediated → re-review CLEARED**
Design Review (self-owned) passed; re-skin/token-parity/reduced-motion all verified genuine. Hostile-input probing found **[B1 BLOCKER] stored CSS injection via tenant font stacks** — font strings reached the emitted `<style>` verbatim on the white-label production path; a `}` breaks out of the rule and injects app-wide CSS for every viewer of that tenant. Confirmed with three live payloads.
**Remediation:** font-grammar whitelist gate (`src/lib/skills/brand-kit/font-stack.ts`) in the shared `resolveAndValidateTokens` pipeline — hostile stacks throw → Signal fallback (matches malformed-color behavior), covering both the `<style>` string sink and the inline-style path, and both `buildBrandKit` + `reviseKit`. 17 regression tests + doc correction.
**Focused re-review 2026-07-07: B1 CLEARED.** All three original exploits + a broad bypass hunt (CSS `\7d` escapes, NBSP, zero-width, RTL override, fullwidth punctuation, `expression(`, 5000-char stack) defeated against the real bundled engine — no run emits a second `{`/`}`, `<`, or `url(`; legit stacks byte-unchanged. 369 unit tests green. Latent sibling (hostile `typography.scale` via future M7 direct `buildBrandKit`, unreachable via `tenants.theme`) confirmed out of scope → Phase-1 follow-up ticket. **F2 code side clear for operator sign-off; freeze awaits operator (resolution 4b).**
Minors carried to Phase 1: named-CSS-color test guard, id-sanitization defense-in-depth, logo_url/custom_domain untrusted-field note, typography.scale grammar gate before M7.

**2026-07-07 · 0.3 F1 data model · QA isolation suite → GREEN (360/360, zero successful attack vectors) · Code Review (security + isolation) → PASS (0 blockers, 1 major, 3 minors)**
Security/isolation core SIGNED OFF: no cross-tenant leak, no unforced RLS, under 360 suite tests + 13 independent reviewer probes (composite-FK attacks, re-homing, forged claims, verdict-nulling sneak-past — all blocked). Freeze criteria 1–3 MET; criterion 4 PARTIAL (contract must be corrected + published by documentation agent); criterion 5 security portion signed off.
**[CR-MAJOR 1]** Schema permitted `automation_level='auto'` on `site_changes` (a fully-autonomous on-page publish path), contradicting doc 00 §2 / CLAUDE.md rule 5 / doc 04 §6 — and the DRAFT contract overclaimed that the schema prevented it. Disposition: docs are not silent → CHECK constraint excluding 'auto' from site_changes + contract §6 correction, dispatched to lead-backend-data-architect 2026-07-07; QA updates its blessing test; documentation agent publishes corrected contract; focused re-review then closes the gate. Surfaced for operator ratification at Freeze Gate 0 regardless.
**Remediation complete 2026-07-07:** CHECK landed (`site_changes_automation_level_allowed` excludes 'auto'; approval gate simplified to `site_changes_requires_approval`, no exemptions); QA suite updated and green at **363/363** (supersedes the 360 pre-remediation count — the exemption blessing became four prohibition tests, future-table tripwire now sweeps all four roles); contract corrected + **published v1.0.0** by documentation agent (criterion 4 MET; sync-verified against SQL, two §5 drift items fixed); TS mirror narrowed (`SiteChangeAutomationLevel` excludes 'auto') per documentation escalation, applied by orchestrator.
**Focused re-review 2026-07-07: PASS — GATE CLOSED.** Zero findings remaining. Original MAJOR 1 construction re-attempted on a fresh scratch DB as superuser: rejected on both INSERT and UPDATE routes; full auto-rollback lifecycle still expressible. All five doc 03 §7 freeze criteria explicitly MET. Freeze declaration reserved for the operator at Freeze Gate 0.
Minors: verdict-presence-not-pass note (contract), client_id claim-minting obligation line (contract), all-roles future-table tripwire (QA).

**2026-07-07 · 0.2 Skill libraries · Code Review → PASS (0 blockers, 3 majors, 7 minors) · QA → GAPS-FOUND (2 criticals, 4 minors)**
Suites at review time: 293/293 across 20 files; isolation verified empirically (suites pass with no network namespace); all four SKILL.md hard rules survived adversarial probing. Gate held open pending disposition of: [QA-C1] schema-generation rejection path untested → tests being added; [QA-C2] NaN channel-weight bug in aeo-audit fix prioritization → `Number.isFinite` fix + regression test; [CR-M1] `reviseKit` bypasses build-time structural validation → unified validate pipeline; [CR-M2] compliance seed-vertical overwrite + zero-rules-evaluated pass → overwrite protection + empty-ruleset fails closed; [CR-M3] public `serializeToScriptBlock` accepts rejected drafts → type-restricted to ready results. All five + gate minors dispatched 2026-07-07 to owning agents (schema-generation/aeo-audit → aeo-seo-logic-engineer; brand-kit → lead-ui-ux-designer; compliance → content-production-engineer).
**Gate CLOSED 2026-07-07:** all 5 criticals/majors + all minors remediated and regression-tested (regressions verified to fail pre-fix where applicable); re-verification green — 329/329 tests across 23 files (schema-generation 70, aeo-audit 81, brand-kit 87, compliance 91), lint/typecheck/build clean. One shared-contract change (`SpacingTokens.steps` → readonly) applied with orchestrator sign-off per escalation protocol. F2 contrast pair-set policy set by design lead (10 pairs, both surfaces) — carried to 0.4 and the F2 ratification list.

**2026-07-07 · 0.1 Project scaffold · Code Review → PASS** (0 blockers, 1 major, 4 minors).
- Major (process): 0.1 was marked done in this file before the review verdict landed. No file change was required (the review passed), but the standing correction is recorded: **gate status is written from the verdict, never ahead of it.**
- Minors, disposition: env server-only split (fixed — `src/lib/env.server.ts` with `server-only` guard), CI `permissions: contents: read` (fixed), `docs/ops/environments.md` CI-env wording (fixed), worker-types note for Phase 1.3 (documented in `workers/edge-autofix/README.md`; due at 1.3).

## Freeze log (frozen-foundation decisions + post-freeze changes)

**🔒 FREEZE GATE 0 — FOUNDATION FROZEN — 2026-07-08 (operator sign-off).**
The operator reviewed a local prototype build of F2 (`npm run dev` → `/design-system`) and signed off on **both** freezes:
- **F1 — multi-tenant data model** (doc 03): 6 migrations, RLS enabled + forced on all 13 tables, structural tenant-consistency via composite FKs, in-schema approval/review gates, `auto` publishing prohibited on `site_changes`. Code Review security gate CLOSED (0 leaks under 360+ probes); QA isolation suite 363/363. **FROZEN.**
- **F2 — design system + white-label theming** (doc 06): token system, tenant theming engine (untrusted-input-safe, B1 CSS-injection fix cleared), 22 token-mapped components, Recharts language, 5 reduced-motion-gated moments. Design Review passed + operator sign-off (resolution 4b). **FROZEN.**
- Skills (0.2) gate-closed; agent team (0.0) operational.

**Post-freeze change control now in force (CLAUDE.md rule 1):** any change to the frozen data model or design system requires **Orchestrator + Code Review sign-off**, recorded here.

_No post-freeze changes yet._

## Open escalations to operator

_None blocking. The "pending ratification" items below are logged for decision at their named checkpoints (F1 = 0.3 freeze, F2 = 0.4 freeze, operator = Freeze Gate 0 review)._

## Operator ratifications — 2026-07-07 (all four decision groups ratified wholesale)

The operator ratified all four groups. Recorded here as binding; the frozen-foundation change-control rule (CLAUDE.md rule 1) governs any future change.

1. **Auto-publish prohibition (F1 MAJOR 1 fix):** RATIFIED. `automation_level='auto'` structurally impossible on `site_changes`; every on-page/site write is `ai_draft_human_approve` or `human_only`.
2. **Access-control split:** RATIFIED. `platform_owner` reads zero rows through tenant-facing policies (ops via `service_role`); clients/tenant_users/tenants writes are `agency_admin`-only; `operator` writes module tables; `client_viewer` read-only + own-client.
3. **F2 theming policies:** RATIFIED. 10-pair contrast set (both surfaces); light-surface `#f4f6f9`; one-palette-per-tenant; fixed type scale (tenants swap colors + fonts only).
4. **Library default thresholds:** RATIFIED as launch defaults — see the ⚑ revisit flags below.

### ⚑ Thresholds to revisit once we have real client data (GG / real estate / Gate 1a)
These were ratified as reasonable launch defaults but are the ones most likely to want tuning against real-world signal — flagged per operator request:
- **aeo-audit review-velocity thresholds** (hyper-local ≥4 new/30d, recency ≤30d; semi-local ≥1/30d, ≤60d) — pure estimates until we see real review flow per vertical.
- **aeo-audit refresh-window default (90d)** — real cadence is vertical- and client-specific (doc 05 cites real-estate 60–90d); calibrate per playbook once GG's decay pattern is observed.
- **aeo-audit base check weights** (AI-crawler access + schema highest) — revisit once we can see which checks actually move citations for a real client at Gate 1a.
- (Not data-dependent, no revisit expected: schema-generation date-match exemption + price-candidate policy; compliance TCPA presence-scope + alcohol-jurisdiction warn; brand-kit contrast pairs. Auto-rollback thresholds are already per-client-configurable — tuned in Phase 1.2, not here.)

## Doc-silent assumptions from 0.2 — RATIFIED 2026-07-07 (superseded by the block above; retained for the decision trail)

**Ratify at F1 (0.3, lead-backend-data-architect):**
1. `tenants.theme.font` sub-shape — doc 03 has singular `font`; doc 06 requires three faces. Library emits `font: {display, body, mono}`.
2. `tenants.theme.colors` key naming — snake_case (`surface_raised`) chosen per jsonb convention.
3. `AutomationLevel` lives as a local copy in aeo-audit `types.ts` — unify into the shared data-layer types at F1.
4. `SchemaBrandContext` identity gap (schema-generation `types.ts` ~467–479) — brand context shape must line up with the `brand_kits` schema.
5. Compliance registry + regex cache are module-level state — must be tenant/request-scoped when wired into the app (flag carried to the 1.x integration review).

**Ratify at F2 (0.4, operator sign-off):**
6. Positive/negative distinguishability policy: hue separation ≥ 30° OR mutual contrast ≥ 1.3:1, auto-separated by lightness (brand-kit `CONTRAST_REQUIREMENTS`).
7. Contrast pair-set policy incl. surfaceRaised pairs — being set by the design lead in 0.2 remediation; carries into the theming engine.

**Ratify at Freeze Gate 0 (operator):**
8. aeo-audit library-defined base check weights (AI-crawler access + schema highest at 12) — playbook schema has no per-check weights; single override point exists if F1 adds them.
9. aeo-audit refresh-window default 90 days (outer bound of doc 05's 60–90 real-estate example), overridable per audit.
10. aeo-audit review-velocity thresholds (hyper-local ≥4/30d recency ≤30d; semi-local ≥1/30d recency ≤60d).
11. schema-generation: date fields are sanity-validated but exempt from visible-text matching (rendered dates can't be deterministically matched) — needs a line in the skill doc.
12. schema-generation: Review/AggregateRating supported as standalone roots (doc 02 profiles list them standalone; SKILL.md showed them only nested).
13. compliance: TCPA check verifies consent-language presence, not legal sufficiency of the consent flow — explicit boundary for compliance-review.
14. compliance: state-by-state alcohol-promotion rules have no matrix in doc 02 → warn with jurisdiction note, never silent pass.

## Resolved escalations (operator resolutions, 2026-07-07)

1. **Phase 1 sequencing (doc 00 §5 vs doc 07):** RESOLVED — **doc 07 is authoritative** for Phase 1 sequencing. Doc 00 §5's ordering is superseded where they differ.
2. **"content pipeline" owner:** RESOLVED — added a 13th agent, **`content-production-engineer`**, owning M8, M9, M11, M12, M15. It generates content; `content-quality` and `compliance-review` remain the independent reviewers — a producing agent never approves its own output. Doc 01 (roster + agent block), doc 05 (module map), doc 07 (1.5/1.7 owners), and `.claude/agents/` updated.
3. **Skills as definitions vs code:** RESOLVED — skills stay as Claude Code definitions; the isolation-tested implementation library lands in 0.2. **The 0.2 gate (Code Review + QA) tests the underlying library, not just the definition.**
4. **F2 design direction encoding:** CONFIRMED as done. **F2 freeze gate:** RESOLVED — Design Review alone is insufficient; the F2 freeze requires **operator sign-off** as independent reviewer. Doc 01, doc 07 (0.4 + Freeze Gate 0), and CLAUDE.md updated.
