# deps & vendored binaries (restored by `npm install`)
node_modules/

# render artifacts (regenerated by gen.js / render.sh)
slides/
output/

# user-supplied source images (keep the repo light; you drop your own in)
assets/uploads/

# generated per-run spec from png-carousel.js (sample.json is the tracked example)
specs/png.json

# scratch
_qa.mjs
